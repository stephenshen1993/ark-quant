# ark-quant Context

> 状态：当前领域词汇与架构协作上下文
> 职责：记录全仓库共用的领域词汇、module 边界和已确认口径，帮助人和 agent 在同一语境下工作。
> 真源范围：工程协作语言和已确认的领域边界。
> 不负责：定义投资规则、修改策略参数、替代正式规则文档或工程决策记录。
> 最后核对：2026-08-06

## 1. 使用原则

本仓库是单一上下文仓库。除非后续文档明确另起上下文，本文件中的词汇和边界适用于整个 ark-quant。

讨论架构时使用以下词汇：

- module（模块）：对外提供稳定 interface、隐藏 implementation 的代码单元。
- interface（接口）：调用方需要依赖的少量稳定能力。
- implementation（内部实现）：module 内部可变化的细节。
- seam（接缝）：适合切分职责的位置。
- adapter（适配器）：把外部形态转换成 module interface 所需形态的薄层。
- deep / shallow（深 / 浅）：interface 小而 implementation 能力强是 deep；只转发、只改名、只搬位置是 shallow。
- leverage（复用收益）：一次设计被多个调用方或多个变化场景使用的程度。
- locality（问题集中度）：相关变化能否集中在少数文件内完成。

## 2. 投资体系词汇

不要把资产类别、策略和账户混成一个概念。

- 投资体系：长期目标、组合结构、资金分配和执行纪律。
- 策略：账户内的选取、持有、退出和调仓逻辑。
- 账户：资金与持仓所在的托管和执行位置。
- A/B/C 组合：投资体系中的顶层组合分层。
- 主动组合：包含广发账户、华泰账户和资金账户的组合层级。
- 广发账户、华泰账户：主动组合下属的证券托管与交易账户，不与主动组合并列表达。
- 资金账户：主动组合下属的现金流解耦账户，是其他账户或组合资金转入与转出的共同边界，也是面向用户表达该账户实体时的唯一名称。资金转入后不再保留来源归属，后续转出也不追溯来自哪一笔入账；避免把账户实体称为“现金池”或“资金池”。
- 资金调拨：账户或组合与资金账户之间的一次单向资金移动动作。转入资金账户与从资金账户转出是彼此独立的调拨，不组成来源账户到目标账户的端到端链路。
- 资金调拨计划：为执行至少一项资金调拨而形成的计划，唯一目的是指导用户完成资金移动，使后续账户交易计划具备资金条件。每项动作的核心要素只有调拨路线、调拨金额和资金可用时间；没有资金调拨时该计划不成立，它也不匹配资金账户两侧的转入与转出来源。计划按资金可用时间组织动作，而不是按来源或去向串联动作。页面使用一张连续表格，只呈现调拨路线、调拨金额和资金可用时间；用途、影响、再平衡原因和下游交易说明不进入执行表。金额紧邻调拨路线，行高只由本行动实际内容决定。
- 资金余额：资金账户中保留为现金或低风险流动性资产的同质化金额事实；余额内资金一视同仁，不按来源账户或入账批次区分。避免使用“现金池余额”作为面向用户的名称。
- 策略可执行预算：某证券账户在执行日可用于本策略订单的金额，由账户原有可用现金、当日可用的资金账户调入和同账户计划卖出回款共同构成；未来才可用的调拨或回款不属于本次预算。
- 账户内交易定额：在一个证券账户内，以策略可执行预算、当前持仓、固定目标集合和价格事实共同确定计划后目标持仓及净订单的组合构造问题。股票和可转债共享这一问题，不按资产类别各自定义资金利用逻辑。
- 策略定额约束：账户内交易定额在某一策略下必须遵守的目标集合、退出条件、交易单位、买入资格和普通订单门槛等差异化规则。两个账户的普通交易计划都以合格 Top 20 为前提，不足 20 时暂停而不静默降级。
- 单标的风险防线：防止某一目标标的因交易颗粒度或定额选择而显著偏离 Top 20 等权目标的共同约束。统一上限为理论等权的两倍，即扣除候选费用前、已冻结策略可执行预算的 10%；若最小交易单位本身超过该线，报告容量冲突而不按资产类别另设阈值。费用仍进入现金约束和计划后权重，但不反向缩小灾难性集中上限。
- 必要卖出：由策略硬退出、目标集合退出、风险失效或已启用风险防线明确要求的卖出；它先于账户内交易定额确定，不以资金利用率或持仓整齐程度为由新增。
- 余现买入修补：在必要卖出与普通净订单确定后，只通过新增合规买入使用仍可投资的账户预算的过程。它不为使用余现或改善表面等权额外制造普通卖单。
- 目标态余现修补：在目标集合已按等权目标和交易单位取整后，仅在不恶化正式等权目标的候选中，将仍可投资的预算补给合格目标标的。等权是首要目标；若下一笔合规买单会扩大目标持仓平方等权误差，应保留该笔自然余现并明确归因为等权优先。
- 订单定额硬约束：目标集合、可交易性、当日真实可用资金、净订单交易单位和已启用的单标的风险防线。已有零股余数在风险减仓后可以保留，但净卖出量仍须满足交易单位；违反任一项的目标持仓组合不可作为交易计划候选。
- 订单定额优化目标：在满足硬约束的候选组合之间，首先比较 Top 20 目标持仓的等权程度，再比较包含现金的整体偏离、资金利用率和预计摩擦成本。后续实施目标不得牺牲首要等权目标。
- 全局离散定额：在固定目标集合和订单定额硬约束内，为各标的同时选择离散目标手数的过程；它按组合整体的订单定额软目标权衡，而不把每一手视为彼此独立的局部决定。
- 订单定额偏好规则：在算法内部从多个满足硬约束的目标手数组合中自动选出唯一结果的、公开且可版本化的比较规则。候选组合不是多份用户计划；对外始终只生成一份完整计划。该规则必须同时呈现等权偏离、资金利用率和预计摩擦成本，不能把每日取舍留给人工临时选择。
- 订单定额目标优先序：满足硬约束后，依次最小化 Top 20 目标持仓平方等权误差、最大整体偏离、全体整体偏离、无归因可投资余现、预计摩擦成本和普通订单数；最后才以冻结的策略排名与证券代码打破技术性平局。同一优先级不能以隐含的实现顺序替代明确比较。
- 无归因可投资余现：扣除必要费用、冻结和不可交易金额后，未配置到目标标的的账户现金。它不是账户内现金目标，而是在次级组合比较中理论权重为零的虚拟仓位；其原因可以是等权优先或不足下一交易单位。候选不足 20 或最小交易单位不可承载属于容量冲突，必须暂停普通计划，不能把该金额包装为余现。
- 整体等权偏离：计划后各目标标的估算权重相对其理论等权，以及无归因可投资余现相对零权重的共同偏离。它是目标持仓平方等权误差之后的次级目标：先最小化所有目标标的与余现仓位中的最大偏离，该值相同才最小化全部偏离总量，不能以平均结果掩盖单债明显失衡或大额闲置现金。
- 当前真源：已经被确认采用的规则或工程事实。
- 实现差距：代码、页面、数据或文档与当前真源不一致的地方。

## 3. 计划生命周期 module

计划生命周期 module 的边界已经确认：

- 负责计划从生成到完成、失败或失效的生命周期事实。
- 负责计划状态、阶段推进、错误记录、订单批次登记和读取。
- 不负责资金调拨算法本身。
- 不负责小市值股票或多因子可转债的订单 sizing 算法本身。
- 不负责定义投资规则。

计划生命周期的主 interface 采用状态机，阶段日志作为审计细节。

主状态包括：

- `draft`：尚未生成。
- `running`：生成中。
- `complete`：已完成。
- `failed`：失败。
- `stale`：已失效，需要重新生成。

阶段日志用于解释状态来源，例如准备账户事实、生成资金调拨计划、生成转债订单、生成股票订单、写入订单批次、完成或失败。

订单批次属于计划生命周期事实。订单内容由外部资金调拨 module 或策略 sizing module 产出，计划生命周期 module 只登记、保存和读取。

第一阶段治理先收敛入口和 interface，不改数据库表结构。

## 4. 订单 sizing 编排 module

订单 sizing 编排 module 的边界已经确认：

- 负责从计划现金、策略榜单、持仓事实和报价生成订单草案。
- 负责调用账户内策略 sizing implementation。
- 负责订单后的现金约束校验和策略订单落库。
- 不负责资金调拨算法。
- 不负责计划生命周期状态。
- 不负责定义股票或转债 sizing 算法。
- 不负责记录成交事实。

HTTP 路由是订单 sizing 编排 module 的 adapter，不承载 sizing implementation。

## 5. 计划生成用例 module

计划生成用例 module 的边界已经确认：

- 负责生成一份完整计划的应用层编排。
- 负责准备计划日、账户事实、市场温度和策略榜单输入。
- 负责校验计划输入窗口。
- 负责调用资金调拨 module 和订单 sizing 编排 module。
- 负责推进计划生命周期状态，并把失败归因到生成阶段。
- 负责组装页面和 API 使用的计划结果。
- 不负责资金调拨算法本身。
- 不负责小市值股票或多因子可转债的订单 sizing 算法本身。
- 不负责定义账户、策略、组合的领域真源。
- 不负责 HTTP adapter。
- 不负责 SQLite schema 细节。

计划生成用例 module 的 interface 应以用例为中心，而不是暴露内部 helper 顺序。当前候选能力包括预览当前计划、生成完整计划、按策略生成订单和读取已生成计划。

生成完整计划时，计划生成用例 module 负责确保计划日所需的策略榜单就绪。该责任保留为用例行为，但必须通过策略运行 adapter seam 完成；计划生成用例 module 不应依赖 HTTP router 来触发策略运行。

第一阶段计划生成用例治理应形成独立 module，而不是只在旧 `plan_service.py` 内改名。`plan_service.py` 可以保留兼容入口和过渡 helper，但新调用方应依赖计划生成用例 module 的 interface。

`plan_service.py` 在第一阶段是兼容与过渡 module：它可以继续提供历史入口、错误类型和可继续下沉的共享 helper，但不再是新计划生成调用方的主依赖。计划页 API 应通过计划生成用例 module 进入预览、完整生成和按策略测算。

## 6. 策略运行 adapter module

策略运行 adapter module 的边界已经确认：

- 负责显式运行账户内策略。
- 负责为指定计划日确保策略榜单已经就绪。
- 负责同一进程内的策略运行锁。
- 负责校验本次策略运行是否产生了完整持久化结果。
- 负责区分策略运行中、持久化缺失、榜单日期错配、数据源失败和策略执行失败。
- 不负责计划生成编排。
- 不负责 HTTP adapter。
- 不负责资金调拨或订单 sizing。

计划生成用例 module 需要补齐榜单时，只能依赖策略运行 adapter module，不能依赖排名 HTTP 路由。排名 HTTP 路由也应通过同一个 adapter module 触发显式运行，保持页面手工运行和计划自动准备使用同一套运行锁、持久化校验和错误分类。

## 7. 账户事实 read-model

账户事实 read-model 的边界已经确认：

- 负责表达账户事实、账户内策略承载关系和 A/B/C 组合层级。
- 账户是资金与持仓所在的托管和执行位置。
- 策略是账户内的选取、持有、退出和调仓逻辑。
- 组合是投资体系层级。
- 当前一一承载只是实现事实，不是长期架构约束。

新调用方需要账户事实、账户角色、承载关系或组合层级时，应使用 `app/account_read_model.py` 的 read-model interface。

legacy `strategy` 命名只保留在旧 DB/API、策略运行语义和 read-model 的 `legacy_adapter` 中。它不是账户事实的新概念真源。

## 8. Web 产品体验词汇

- 全站信息架构重设计：本轮从用户的投资任务、信息对象、状态与流转关系出发，自顶向下重新推导 Web 工作台的信息架构，不以当前页面数量、导航入口、全局状态条、页面名称、页面顺序或区块布局为约束。当前三页实现与既有界面结论只作为现状证据和待验证假设，可以被合并、拆分、改名、重排或取消；投资体系真源、账户/策略/组合边界、账户数据语义、计划领域对象、系统判断与人工执行边界等已经确认的领域事实继续有效，不因信息架构重设计而重开。
- 投资概览职责：Web 工作台除支持计划准备、生成、复核与人工执行外，也承担让用户了解当前整体投资状态的概览职责；它不是单纯的计划生成器。概览需要与投资工作流共同纳入顶层信息架构，但概览的具体内容、层级和入口仍需在本轮自顶向下重新确认。
- 概览与计划的独立性：投资概览和计划是两个彼此独立的用户任务与信息空间。概览只回答“整体投资现在怎样”，围绕当前配置结构及其目标偏离建立全局认知；计划只回答“下一步做什么”，围绕计划输入、系统判断、资金调拨和账户交易安排组织。概览不充当计划流程的开场摘要，计划也不承担整体投资概览；两者不得在同一首页或页面中拼接成上下两层，也不互相复制状态、卡片或明细。必要的关联只通过明确的下钻或跳转建立。
- 概览、计划与榜单一级入口：全站将“概览”“计划”和“榜单”设为彼此独立的投资工作入口，不再使用一个混合首页同时承载概览、计划状态和策略信号。概览入口拥有整体配置结构与目标偏离的主内容；计划入口拥有下一步计划的准备、判断、复核与执行安排；榜单入口负责查看、生成和导出股票/转债策略信号。跨入口只提供必要的明确跳转，不设置共享主内容区。
- 计划入口命名：一级入口正式使用“计划”，不再使用“今日状态”。计划工作区可能处于准备、生成、复核、未来待执行、失效或失败状态，并不都发生在今天；页面内部再根据当前阶段使用准备计划、计划待执行等具体标题。
- 取消全局状态与导航提醒：全站不再跨页面展示计划基准日、计划就绪度、生成状态或执行阻塞的全局状态条，一级导航也不使用提醒点、数量徽章或“尚未查看”等任务状态。导航只负责稳定地进入概览、计划、榜单和账户，不承担任务收件箱；概览不显示计划状态，榜单不显示计划执行状态，账户只表达各账户自身数据，更新日志也不显示投资工作状态。完整计划状态只属于计划入口，账户数据没有统一到期提醒，未保存草稿通过离开保护处理。
- 计划生成临时通知：若计划生成允许用户离开计划页，完成或失败时可以提供一次性全站临时通知并允许点击返回，但不得把临时结果积累成长期导航红点。用户主动进入计划页时再完整查看当前状态；系统不因当前没有计划、输入已经就绪、计划失效或用户尚未打开某版本而持续催促。
- 默认入口：Web 工作台默认进入“概览”。用户打开系统时先查看整体配置状态，再按需要主动进入计划或账户工作区；默认进入概览不改变概览与计划的独立性，也不授权把计划状态、阻塞或执行安排重新拼入概览。
- 一级导航顺序：一级导航稳定使用“概览 → 计划 → 榜单 → 账户”的顺序，对应先了解整体、再处理下一步计划、按需查看策略信号、最后查看或维护具体账户；更新日志继续位于次级系统信息区域，不插入投资工作区之间。
- 当前计划周期：计划一级入口的主内容只围绕当前正在准备、生成、复核或等待执行的计划周期组织，持续回答“下一步做什么”。失效计划和旧计划不与当前计划混排，也不以日期列表或计划档案库作为计划入口的主要结构；历史计划如需追溯，通过次级“历史计划”入口访问。
- 历史计划防误执行：历史计划只从计划工作区的次级入口访问，打开后在顶部明确标记“历史计划，仅供追溯”，并完整保留原始计算范围、数据日期、执行日期、生成时间和版本。正文可以完整查看，但不提供生成、修改或任何当前执行类操作，只提供清晰的“返回当前计划”；历史计划不触发一级导航提醒，也不与当前计划混排。
- 历史计划时间组织：历史计划列表以适用执行日作为主时间倒序组织，每条同时显示是否包含组合间再平衡、生成时间、短版本号，以及已过执行窗口、已失效或生成失败等最终状态。生成时间只用于区分同一执行日的不同版本，不取代用户按“某天原本应该如何操作”进行追溯的主心智。
- 历史计划版本分组：同一执行日存在多个版本时按执行日归为一组，列表默认只突出该日最后生成的版本，并以“共 N 个版本”提示可展开查看更早版本。所有版本继续保留且不得静默覆盖，但旧版本不与最后版本平铺竞争注意力。
- 计划阶段驱动：当前计划页以一个明确的主阶段组织体验，首先说明当前处于准备、生成、复核、待执行、失效或失败中的哪一种状态，只强化当前阶段需要的信息和主操作。已经完成的阶段压缩为可核对摘要，尚未到达的阶段不提前铺开；计算依据和高级工具始终退为次级内容，不与当前状态、下一步和计划结果竞争。
- 执行窗口结束后的入口状态：计划执行日早于用户本地当前日期后，该计划自动退出计划入口主内容并归入历史计划；计划入口恢复为新一轮准备状态，默认仅做组合内轮动。顶部只用一行说明“上一计划执行窗口已过”并提供历史查看入口，不继续展开旧交易明细，也不把正常时间流逝渲染成红色故障。新计划仍必须通过当前范围所需的最新账户数据校验，避免沿用旧事实重复生成旧动作。
- 当前计划失效后的呈现：账户数据或其他计划输入变化导致当前计划失效时，主区域明确说明“输入已变化，原计划不可再执行”，默认不再展开旧资金调拨和证券交易明细。旧版本通过“查看失效版本”进入只读历史视图；主操作是按当前输入重新生成新版本。失效表示原输入已变化，不得与生成失败共用同一错误文案或状态语义。
- 重新生成失败与旧版本：失败的生成尝试不得覆盖或篡改旧计划。旧计划输入未变化且执行窗口仍有效时，继续保留为当前可查看计划，并在旁说明最新一次重新生成失败；旧计划输入已经变化时则继续保持失效，只能历史查看，不能因新生成失败而恢复。失败区域只显示可理解的原因、受影响部分和“重试生成”，不得把失败的新结果与旧计划拼接成一份混合计划。
- 完整计划原子性：本次范围内的资金调拨、账户资金闭合或任一必要证券交易部分生成失败时，整份新计划都标记为“生成失败，不可执行”。已经算出的局部结果只用于诊断，不以正常执行表格或可截图计划呈现，也不得与旧版本拼接；独立硬风险提醒可以另行显示，但不能伪装成一份部分成功的完整计划。
- 计划流程终点：方舟计划的职责止于形成并展示可靠的待人工执行安排，不记录用户已经在外部券商完成交易，也不把手工勾选或页面操作当成真实成交事实。实际成交与持仓变化由后续账户数据更新反映；计划界面可以表达待人工执行、尚未到执行窗口或执行窗口已过，但不得声称交易已经完成。
- 计划准备阶段：计划尚未具备生成条件时，主区域只列当前缺失、过期或失败的前置条件，每个阻塞项说明其影响并直达真正的维护入口。已经就绪的输入压缩为一行准备完成摘要，按需展开核对；计划页不嵌入账户编辑器、策略榜单或大块输入数据面板，避免把“还缺什么”变成内部输入清单。
- 计划范围驱动的前置条件：前置条件使用本次范围真正需要的最小依赖集。仅做组合内轮动时，只校验 A 组合相关账户、资金余额、全市场温度和相关策略数据，B/C 账户与 B 申购能力不得阻塞；用户选择同时进行组合间再平衡后，才校验 A/B/C 全部相关账户。B 申购能力仍只在初步结果确实需要买入 B 时成为必要条件。用户切换范围后立即重新计算阻塞清单，页面只显示当前范围实际需要处理的事项。
- 计划阻塞修复回路：用户从计划页进入某个阻塞账户时，系统保留本次计划范围、来源页面和阅读位置，并直达该账户编辑视图。账户保存成功后提供明确的“返回计划”；返回时恢复原计划上下文并自动重新检查前置条件，不把用户丢到账户总览，也不因账户保存或条件恢复而自动生成计划，最终仍由用户明确点击“生成计划”。
- 计划计算范围：每份计划固定包含 A 组合内轮动，用户唯一需要决定的是本次是否额外进行组合间再平衡；这不是两种并列计划，更不是多种触发原因或五类计划。计划不记录季度、临时、市场异常或 B 申购能力恢复等触发原因；系统不得把代码中的 `monthly_contribution`、`b_recovery` 或 `ad_hoc` 等实现分支包装成用户计划类型。全市场温度与策略榜单属于系统输入；策略榜单作为“榜单”工作区只读查看、生成和导出，计划页只消费榜单结论，不嵌入大块榜单表格。
- 计划范围包含关系与决策权：组合内轮动是每次计划都包含的基础动作，计算 A 组合内部小市值、可转债和资金余额之间的轮动。用户选择同时进行组合间再平衡时，系统先计算 A/B/C 组合间再平衡，并继续计算必要的 A 组合内轮动、资金安排和账户交易；组合间再平衡不单独生成一份与组合内轮动并列的计划。是否加入组合间再平衡完全由用户决定。
- 计划范围选择交互：计划准备页不再让用户在两个计划类型之间二选一，只提供一个“同时进行组合间再平衡”的附加选项，默认关闭；关闭时仍会生成完整的组合内轮动计划，用户需要时主动开启以扩大计算范围。现阶段不引入系统范围推荐、到期任务或周期提醒；点击“生成计划”即确认并冻结本次范围，已生成计划只记录是否包含组合间再平衡，不记录触发原因。
- 已生成计划的范围表达：一级入口和页面名称始终是“计划”，不为是否包含组合间再平衡拆出并列入口或两套页面。计划身份区域只记录本次是否包含组合间再平衡；正文按实际执行动作组织，不把范围重新包装成互相竞争的计划卡片。
- 已生成计划的执行顺序：是否出现资金调拨正文只取决于本次是否存在真实资金调拨动作，与是否包含组合间再平衡没有一一对应关系；仅做组合内轮动也可能需要账户间调拨。有调拨时先显示一张精简的资金调拨表，再显示各账户交易计划；无调拨时只用一行“无需资金调拨”交代结论，不保留空章节或空表格。
- 新增资金与 B 申购能力：新增长期资金到账后直接更新资金账户余额，它已经成为账户事实，不再作为计划条件重复录入“新增本金”金额，也不构成独立计划类型。B 组合能否申购及可申购额度属于 B 组合自身的当前执行能力属性，由用户提前维护、只在实际变化时更新；计划生成时直接读取，不要求用户针对每次计划重新填写或确认。该状态不是计划类型，也不能与 A 组合内轮动或组合间再平衡并列。
- B 申购能力最小模型：系统区分未维护、不可申购和可申购三种状态；选择可申购时必须维护当前实际申购上限。更新时间由系统在保存时自动记录，核验来源默认沿用上次值并作为次级证据信息按需修改。概览主界面只突出“可申购及上限”或“不可申购”和更新时间；计划按实际上限约束 B 买入，不把来源等核验字段带入主流程。
- B 申购能力有效性：不设置任意的固定有效天数。维护状态时允许按真实外部条件选填“有效至”；有明确有效期的状态到期后，仅在本次计划实际需要买入 B 时阻塞并直达概览中的 B 状态维护。没有明确有效期时，状态持续有效直至用户更新，系统通过核验时间表达其年龄，可提示较旧但不得擅自判定失效；计划需要买入 B 时清楚展示状态、上限和核验时间，用户点击“生成计划”即接受并冻结该输入，不再增加独立确认步骤。仅做组合内轮动或无需买入 B 时不以该状态打扰用户。
- 计划生成触发：所有前置条件就绪后，计划仍由用户明确点击“生成计划”触发。点击时冻结本次输入并形成可识别版本；打开页面、更新单个账户或后台依赖恢复不得静默生成计划。已有计划时重新生成必须作为明确操作，并提示将形成新版本。
- 已生成计划首屏：已生成计划首先只给出一个明确结论，使用无需操作、需要执行、计划待执行或计划不可用等互斥表达；数据基准日、执行日、生成时间和版本紧邻结论形成计划身份。计划页顶部只保留一层 masthead：标题、状态、数据基准日、执行日、生成时间、版本和专注视图入口在这一层完成表达，不再另起第二条状态横线或独立 hero 重复同一组日期与状态。有行动时，结论后直接进入资金调拨计划和账户交易计划，不再重复展示计划就绪度卡片、状态链或多组状态徽章。
- 已生成计划不可变：已生成计划作为本次算法输出保持只读，资金调拨金额、证券列表和交易数量不能在页面原地修改。计划页只提供复核、展开、截图和查看依据；需要改变结果时必须修改相应输入或规则后明确重新生成，并形成可识别的新版本，不能静默覆盖旧计划。外部券商实际成交差异由后续账户数据反映，不回写篡改原计划。
- 计划连续文档：整份已生成计划以一份连续的执行文档呈现，而不是由多层卡片、仪式化判断大卡和嵌套表格拼成。计划身份只出现一次，并以紧凑 masthead 表达计划状态、数据日期、执行日期、生成时间和版本；资金调拨计划构成一个章节，账户交易计划按账户构成后续章节；账户摘要与证券交易明细属于同一账户章节。模块内部只表达模块特有信息，不重复全局执行日期；只有资金可用日或账户交易日偏离全局执行日时才在对应行显示日期。正常内容主要依靠标题、分割线、列对齐和有节制的留白建立层级，卡片只用于异常、准备态阻塞或必须独立强调的信息。
- 账户交易计划主信息：账户交易计划的主语是具体证券买卖交易，核心信息是证券、买入/卖出方向、交易数量和交易单位；最终效果才体现为账户持仓数量变化。执行表必须让每行脱离分组标题也能独立读懂，因此买入/卖出方向是逐行显式字段；交易数量保持为数量加单位的单行主信息，不把方向混入数量列。清仓、减仓、建仓和加仓是这笔交易对应的仓位语义，用于解释当前持仓到目标持仓的变化，并作为同方向内部的执行优先级：清仓先于减仓，建仓先于加仓；它可以用窄列轻量展示，但不能压过“买入多少 / 卖出多少”的可执行指令。
- 账户交易计划折叠摘要：账户交易计划折叠态必须像桌面工作台列表一样可扫读，直接表达账户名、资金前提、动作分组数量和计划后资金余额；普通资金前提不使用高权重提示块，只有阻塞或异常才使用警示色块。摘要句使用执行语言，例如“需先调入 ¥X；卖出 N 笔，买入 M 笔；计划后资金 ¥Y”，不再把全局执行日塞入账户摘要；账户状态徽标默认只显示“资金已具备 / 需要当日调入 / 等待未来资金可用”，仅当对应日期偏离全局执行日时附带日期。展开账户后优先呈现证券交易明细，参考价口径、硬约束和资金估算过程属于辅助依据，默认不得压在交易表之前成为主阅读内容。展开态表格上方只保留账户序号与收起/切换控制，不重复展示折叠态已经表达的账户名、策略、资金状态、执行日期和计划摘要。
- 账户一级入口：“账户”作为与概览、计划并列的独立一级工作区，负责查看和更新各账户当前数据。账户数据同时服务概览与计划，不归属于其中任何一方；概览和计划发现账户数据问题时只提供直达对应账户的入口，不在各自页面复制账户维护表单。
- 账户工作区内容边界：账户工作区彻底按实际托管账户组织，只列广发、华泰、资金账户及其他真实账户，并显示各账户当前金额、数据时间和自身数据状态；选中账户后直接查看和更新现金、持仓等当前数据。A/B/C 组合、策略配置和目标偏离全部属于概览，整份计划是否就绪全部属于计划，账户页不再展示这些跨账户汇总。
- 账户工作区主从结构：桌面端左侧保持实际账户列表，只显示账户名、当前金额、数据时间和该账户自身状态；右侧直接打开最近使用的账户，并以预填完整当前数据的可编辑视图作为主体，不增加账户汇总卡片或必须再次点击的“编辑模式”。从计划阻塞项跳转时覆盖最近使用记录，直接打开被点名账户。手机窄屏使用明确可见的账户选择控件，不横向堆叠账户列表。
- 账户列表排序：账户默认按当前金额从高到低排列，不提供拖拽排序或个性化顺序。编辑期间列表位置保持不变，保存成功后才按新金额重排；金额相同时按固定账户标识保持稳定，金额暂不可计算的账户排在已知金额之后，同时继续清楚标明其数据或估值异常。
- 账户状态语义：账户页只表达账户自身事实，包括数据是否存在、原始金额/现金/持仓是否完整、行情估值是否可用、最近更新时间、未保存修改和保存冲突；不再长期展示“可用于当前计划”或“需要为计划更新”等计划语义。某账户不满足本次计划输入窗口时，由计划页点名并直达该账户处理。
- 更新日志导航层级：更新日志属于系统信息，不与概览、计划和账户三个投资工作区平级。入口固定放在左侧导航底部的“系统信息”区域，以“更新日志”文字入口常驻可见；不藏入头像菜单，也不插入三个一级投资工作区。日志内容继续完整保留，不因降低导航优先级而改造成隐藏的工程审计页。
- 投资概览核心：概览围绕“当前资金如何分布、是否仍符合投资体系的目标结构”组织，而不是以总资产金额或资产清单为中心。默认需要表达顶层 A/B/C 组合的当前配置与目标配置，以及 A 组合内部小市值股票策略、多因子可转债策略和资金余额的当前配置与目标配置；总资产金额只作为理解配置的辅助基数。账户金额、具体持仓、收益率和净值曲线不因容易展示而混入概览；长期绩效继续服从独立记录真源。
- 投资概览判断边界：概览只描述当前配置、目标配置与偏离程度，可以表达接近目标或偏离明显等结构状态，但不把偏离直接解释为需要调仓，也不生成或暗示具体资金调拨和证券交易动作。是否行动必须在计划工作区结合阈值、检查周期、资金可用性和策略约束重新判断；只有计划负责回答具体下一步。
- 投资概览数值主次：概览以当前占比和目标占比作为配置结构的主尺度，以当前金额和目标金额作为次级精确值。偏离可以同时提供百分点和金额，但不得在视觉层级上压过当前与目标；全体系总资产只出现一次，作为相关金额的共同计算基数，不扩张为资产净值看板。
- 投资概览温度信息：概览展示决定动态目标配置的全市场温度，并同时标明温度的数据日期与来源。温度是市场状态和计划输入，不是账户事实，也不属于任何单个账户；概览和计划必须读取同一当前温度口径，不得把账户 context 中历史保存的温度当成当前温度展示。温度只作为解释“目标配置为何如此”的核心因果变量，放在目标结构附近且不压过当前与目标配置；策略榜单、筛选过程、计划输入条件和其他行情指标不因此进入概览。
- 投资概览数据时间：概览使用各账户独立维护的最新已知数据形成整体结构，不要求所有账户同时更新，也不把它们伪装成同一时点快照。每个组合或策略保留可追溯到底层账户的数据时间；缺失或明显不再可靠的部分标为待确认，其占比和偏离不得表现成精确结论。概览的数据可信度是概览自身的表达状态，不直接复用计划输入窗口或计划就绪状态。
- 投资概览交互边界：概览中的资产结构、目标、偏离和账户派生数据保持只读，不直接编辑账户数据，也不生成或重新生成计划。B 组合的外部申购能力是唯一明确的上下文维护例外：在 B 组合旁显示可申购状态、额度和更新时间，点击后以轻量浮层更新；不为这一项新增独立工作区，也不把它塞到账户页。计划页只读取该状态，缺失时可直达此处维护。其他组合或策略点击仍只用于查看更多解释或下钻，不触发隐式修改或计划操作。
- 投资概览层级边界：概览止于组合与策略结构。顶层表达 A/B/C 组合，并在 A 组合内表达小市值股票策略、多因子可转债策略和资金余额；不继续展开到广发、华泰等托管账户，也不展示具体证券持仓。账户与持仓属于独立数据工作区；账户数据缺失或不可用于计划时，只作为工作周期阻塞提供下钻入口，不把账户维护内容搬进概览。
- 个人投资系统：`ark-quant` 的系统身份。它把用户个人投资体系中的规则和算法固化为可复核的账户数据、判断与执行计划，以提高决策效率、降低执行阻力；它不替用户交易，最终决策与成交仍由用户完成。
- 投资决策工作台：个人投资系统的 Web 交互界面，不是独立品牌。它帮助用户更新账户数据、判断计划是否就绪、理解执行安排并完成复核；优先追求可信、清晰、可扫描和行动确定性，不以内容展示或通用后台管理为主要目标。
- 系统命名：面向用户的主名称是“方舟计划”，身份说明是“个人投资系统”；`ark-quant` 是仓库名和技术标识，不承担品牌表达。界面不虚构独立品牌或使用含义不准确的内容产品副标题。
- 决策与执行边界：界面明确区分账户数据、系统判断和待人工执行三层信息。计划或订单生成只表示建议已经形成并可供复核，不表示已经下单或成交；实际交易始终由用户在外部券商端完成。
- 全站信息类型：投资决策工作台在全站视觉与文案中稳定区分数据、判断和执行三类信息。数据包括资金余额、持仓、各账户数据时间和账户层级；判断包括无需操作、需先调拨、计划已失效等系统结论；执行包括资金调拨动作、账户交易计划和证券交易明细。预计资金余额等计划推导结果不得表现成真实账户余额，计划生成也不得表现成已经下单或成交。
- 账户界面命名：主导航使用“账户”，页面标题使用“账户数据”。“账户事实”和“账户快照”只用于系统内部的计算、持久化与复核语境，不作为用户任务或页面主概念。
- 无需操作：当规则判断当前不需要调仓或调拨时，“无需操作”是完整、明确的计划结论。界面同时说明判断依据、数据日期、执行日期和计划版本，不把无动作表现为空白、禁用或低价值状态，也不鼓励为产生动作而频繁交易。
- 账户交易计划：某一具体账户在一个计划周期内的完整人工执行单元，包含该账户内策略产生的卖出、买入、现金约束和执行顺序；它是证券交易明细的直接上下文。具体账户是计划的主身份，所属组合与策略是来源说明，不能代替账户身份；没有证券交易明细时该计划不成立。
- 证券交易明细：账户交易计划中的一笔具体买卖，不是跨账户的独立顶层行动；脱离所属账户交易计划不单独承担决策或执行意义。明细的用户可见执行方向只有买入和卖出两类，必须逐行展示；清仓、减仓、建仓和加仓是更细的仓位语义，用于解释交易产生的持仓变化和同方向内的执行优先级，不作为大分组或阶段标题。证券交易明细以一张连续执行表呈现，不在表体插入卖出/买入分组行；默认执行顺序为清仓、减仓、建仓、加仓，同一仓位语义内保留策略生成顺序；每一行仍必须显式给出买卖方向；不设置额外顺序列或可见阶段标题，默认按表格从上到下执行。清仓表示当前持有且计划后归零，减仓表示当前持有且计划后减少但不归零，建仓表示当前未持有且计划后买入，加仓表示当前持有且计划后增加。HOLD 等无需交易状态不进入执行单。
- 账户内执行阶段：账户交易计划内部按执行依赖形成的交易分段；存在卖出与买入时，先释放资金再使用资金，没有证券交易明细的阶段不成立。证券交易明细归属于相应阶段，但阶段是底层计算与执行依赖结构，不作为用户可见的额外层级，也不因此成为跨账户的顶层行动。
- 账户交易计划的资金前提：对该账户能否正常执行交易计划的资金条件判断，区分资金已具备、需要先调入资金和需要等待资金可用。它只描述本账户计划的可执行性，不记录所需资金的历史来源。
- 计划后预计资金余额：账户在预计资金调拨和证券交易全部发生后的资金估算结果，是账户交易计划能否资金闭合的校验，不是实际成交后的余额。余额不足表示计划不可正常执行，不能作为普通提示继续对待。现金闭合公式属于复核过程，不是账户交易计划的折叠态摘要；折叠态只表达资金前提和是否可继续交易，完整估算过程必须退到展开后的次级细节中。
- 计划审阅顺序：先审阅资金调拨计划，再按资金已具备、需要当日调入、等待未来资金可用的顺序审阅账户交易计划；同一资金前提下保持固定账户顺序。审阅顺序用于先确认资金安排和当前可执行范围，不等同于所有资金调拨都先于所有证券交易发生。
- 账户级执行摘要：对本次涉及的账户交易计划及其资金依赖的概括，用于识别执行对象与先后约束，不用于表达任务数量或工作量。避免用全局交易笔数或“步骤数”代替计划判断。
- 需要执行的计划：首屏结论后直接进入资金调拨与账户交易计划。账户交易计划是证券交易明细的直接执行计划，资金调拨动作归属于资金调拨计划；正文只识别执行计划和资金可用时点，不追踪端到端现金流，也不把全部交易明细或调拨动作展平为全局步骤。执行内容按真实依赖区分卖出或释放资金、当日调拨、下单、次日回流等时序；原型和实现必须消费结构化执行阶段与资金可用时间，不能凭前端文案猜测顺序，也不能把次日回流表示为当日可用于买入的资金。计算依据附在对应动作旁渐进展开，不成为与行动割裂的大型独立区块。
- 账户执行卷宗：展开某个账户交易计划后，界面持续保留账户名称、执行日期、资金前提和收起入口，长列表滚动时不得让证券交易明细脱离账户上下文。同一时刻只展开一个账户交易计划，并把展开账户写入可恢复的页面地址状态；它是审阅上下文，不是新的跨账户步骤或独立执行单。
- 证券交易执行保护：参考价只表示计划生成时的估值依据，不是券商限价指令。账户交易计划必须在对应交易明细旁展示价格基准日与适用于该策略的硬约束；可转债买入或加仓必须明确实际成交价低于策略价格上限，股票计划必须明确已通过单只仓位上限与容量校验。硬约束不满足时应跳过或阻断对应交易，不能降级成普通提示。
- 执行窗口有效性：计划的执行日期早于用户本地当前日期时，页面将其标记为执行窗口已过并停止展示为今日可执行建议；旧计划仍可作为内部追溯事实保留，但不得依靠绿色状态、普通计划卡或模糊文案诱导继续执行。未来资金可用的账户计划可以审阅，但必须清楚标为当前不可执行。
- 未来执行计划：计划存在证券交易或调拨，但执行日期晚于用户本地当前日期时，页面将其表达为“计划待执行”，不得继续使用“今日需要操作”。未来计划仍可完整审阅，主时间线应明确连接数据基准日与执行日，并提示未到执行窗口不要交易。
- 计划专注视图：计划入口提供同一份计划内容的专注查看状态，用于桌面复核和截图转发；它隐藏全站导航、重复状态、计划条件与高级操作，让资金调拨、账户资金前提和证券交易明细占据主要画布。专注视图不是另一份手机执行单，不复制或改写计划数据，并写入可恢复的页面地址状态。
- 证券交易明细列：桌面交易表将证券代码与证券名称拆为独立列，避免两类身份信息挤在同一单元格。买卖方向、交易数量和交易单位是账户交易计划的核心执行字段，其中买卖方向独立成窄列并与交易数量相邻，交易数量与交易单位合并为“数量 单位”的单行数值信息；仓位语义使用“仓位”窄列展示清仓、减仓、建仓和加仓，用来解释排序和持仓变化，不命名为“动作”以免与买卖方向混淆。执行表按内容所需宽度收缩，不为填满账户卡片而拉开列距，避免代码/名称与买卖/数量/金额之间出现大段空白。执行前后持仓变化只是交易数量的计算依据，默认不常驻占用第二行，可通过不扰乱密度的轻量说明保留。旧计划缺少前后持仓时只展示已有本次数量，不从动作或差额伪造历史持仓。
- 执行保护呈现：价格硬约束属于账户交易计划的执行保护，在账户卷宗顶部集中展示，不在每一行重复最高价以免降低执行表密度。可转债买入或加仓必须明确实际成交价低于策略价格上限；生成时已通过的账户级仓位或容量校验继续保留在账户卷宗说明中。
- 计划版本可识别性：当前计划在主判断附近持续显示生成时间和短版本标识，确保重新生成后的不同截图可以辨认新旧；事实窗口只用于输入校验或异常说明，不占用当前计划的版本位置。
- 账户数据：面向用户表达某一个账户当前用于计划计算的金额、现金与持仓。用户的任务是更新账户数据，不是创建、浏览或维护一套“历史账户事实”。
- 账户数据时间：某一个账户当前数据各自的截至时间，由系统在该账户保存时自动记录。常规更新不要求用户填写日期或时间；只有明确补录旧数据时才通过次级入口指定历史时间。各账户独立更新，不存在面向用户的全局“账户事实日”，也不要求所有账户同时更新；计划就绪度按每个账户自己的数据时间判断。
- 账户旧数据补录：通过次级入口记录一份早于该账户当前状态的数据，只补充内部追溯，不替代或创建时间更新的当前账户状态，也不写入计划读取的 operational snapshot；即使账户尚无当前数据，补录也必须继续保持当前状态缺失，不改变当前计划就绪度或有效性。修正当前账户数据仍属于正常账户数据更新。
- 账户快照：系统为了计划复核与追溯而保存的单个账户数据版本，只是内部持久化形态。账户快照不作为用户任务，不把多个账户捆成一次全局快照，也不要求它们共享日期。
- 账户数据更新：每个账户独立以自身当前金额与持仓作为编辑起点，用户只录入变化项；目标时间已有其他账户数据，不得让本账户从零开始。只有本账户从未存在数据，或用户明确选择空仓或清零，才使用空状态。
- 证券账户一致状态：同一证券账户的现金与持仓共同构成一个时间一致的当前状态，共享该账户的数据时间。即使用户只修改其中一项，更新后的账户状态也不得拼接来自不同时间的现金与持仓；不同账户仍各自独立。
- 证券账户更新原子性：一次证券账户更新必须同时接受或拒绝该账户的现金与全部持仓，不能形成部分新、部分旧的混合状态。更新失败时，原有当前状态保持不变，用户尚未保存的修改仍作为草稿等待重试。
- 账户更新冲突：用户开始编辑后，如果同一账户已在其他位置形成更新的数据状态，当前旧编辑内容不能静默覆盖新状态。冲突时保留用户草稿，并要求以最新账户状态重新核对后再提交。
- 证券账户数据更新：用户直接在该账户当前持仓上修改数量、添加证券或删除证券；未变化持仓无需重新录入。界面不向用户暴露新建快照、选择事实日、沿用历史或复制旧数据等系统内部步骤，保存前只需轻量标出本次变化。
- 证券账户编辑内容：单账户编辑视图默认完整呈现该账户当前持仓与现金，并预填已有数值，用户进入后即可修改。账户摘要不充当必须先穿过的只读层，也不要求用户再次切换到单独的编辑模式。
- 单只证券清仓：用户将某只证券的持仓数量改为零或删除该持仓，二者表达同一清仓意图。保存后的当前持仓不保留零数量记录；单只清仓属于普通持仓变更，只有全部持仓同时清空才构成需要单独确认的账户清仓。
- 账户内持仓身份：证券代码是单个证券账户内当前持仓的唯一身份，同一代码只能存在一条当前持仓。再次添加已有代码表示定位并修改原持仓，不产生需要另行合并的重复记录。
- 当前持仓排序：证券账户的当前持仓默认按持仓市值从高到低展示；市值相同时按证券代码升序保持稳定，行情暂缺、无法计算市值的证券排在已估值持仓之后。排序只作用于展示，不改变用户原始持仓数据。
- 移动端账户选择：手机窄屏下账户切换必须使用明确可见的选择控件，不把账户列表隐藏在横向滑动区域中；切换仍需遵守未保存修改保护。
- 证券代码识别反馈：用户新增持仓并输入 6 位证券代码后，账户页应立即尝试识别证券并给出“识别中、已识别、价格暂不可用或识别失败”的行内反馈；行情或识别失败不得阻塞原始代码和数量保存。
- 证券账户原始数据：用户只维护持仓证券代码与数量，以及账户的可用现金和冻结现金。证券名称、最新价格、持仓市值和账户总资产均为系统派生结果，不要求用户录入，也不作为与原始数据并列的独立保存对象；legacy 迁移账户也必须在读取时自动派生价格和市值，不得要求用户重新录入或先确认一次才能看到行情。
- 行情依赖边界：证券代码、持仓数量、可用现金和冻结现金等账户原始数据可以独立保存，不依赖行情服务成功。行情不可用时，价格、市值和总资产应明确为暂不可计算，依赖估值的计划生成必须等待行情恢复，但不得阻塞账户原始数据更新。账户页、计划就绪度与计划生成必须消费同一份当前估值；补齐或刷新系统估值后若账户总额发生变化，即使用户原始数据未变，依赖旧估值的计划也必须失效。
- 账户数据确认：当某个账户的金额、现金和持仓均未变化时，用户可以明确确认“数据仍准确”，由系统只刷新该账户的数据时间，不要求修改字段或制造虚假变化。确认是单账户动作，不捆绑其他账户。
- 账户编辑主操作：单账户数据没有变化时，主操作是“确认数据仍准确”；任一原始数据发生变化后，主操作切换为“保存账户数据”。两种意图不同时竞争，也不存在不改变数据的空保存。
- 账户数据变化与计划失效：账户金额、现金或持仓发生实际变化时，基于旧账户状态生成的当前计划随即失效；仅确认数据仍准确且数值未变化时，当前计划保持有效。计划是否失效取决于计划输入事实是否变化，不取决于账户数据时间是否刷新。
- 账户数据缺失与零值：尚未录入、已录入零金额和已确认空仓是三种不同状态。缺失数据不得用零金额、空持仓或目标时间伪装；只有用户明确保存零金额或清空全部持仓，才形成真实零值或空仓。
- 账户清零或清仓：用户明确把金额账户保存为零金额，或删除证券账户的全部持仓。它是区别于普通数据更新的高风险意图，需要用户单独确认；一般金额或数量修改、单只证券增删不属于整账户清零或清仓。
- 证券账户持仓清空与账户清零：删除证券账户的全部持仓只把持仓记录为已确认空仓，可用资金与冻结资金保持不变；只有用户从独立的“清空账户”入口确认后，才同时把现金与持仓清零。两种意图不得共用会连带修改现金的保存逻辑。
- 账户数据保存：用户完成单个账户的数据编辑后，点击“保存账户数据”即明确提交普通更新，不再追加确认步骤；只有整账户清零或清仓需要单独确认。输入失焦、账户折叠、页面切换或导航离开都不代表保存意图；存在未保存修改时应提醒用户处理。
- 账户保存完成态：账户数据保存成功后，单账户编辑视图继续展示该账户更新后的数据时间、派生结果与已保存状态，供用户核对。保存成功不自动离开当前账户，返回账户总览仍由用户主动决定。
- 单账户编辑上下文：用户从账户列表选择一个账户后，右侧主体只承载所选账户的预填数据、本次变化、保存与取消，不捆绑其他账户的数据更新；它是账户工作区的当前主内容，不需要先穿过一层只读账户总览。
- 计划事实就绪度 module：`app/plan_service.py` 负责相对当前计划输入窗口和用户所选计算范围，校验该范围真正需要的账户与系统输入，并通过小型只读 interface 返回计划基准日、输入窗口、整体状态和逐项状态；HTTP adapter 使用 `GET /api/plan/readiness`，后续规格必须补足范围参数或等价上下文。账户 read-model 继续只描述各账户数据，计划页消费就绪度 interface，不调用整份计划来旁路取值，也不在前端复制窗口或必备账户规则。就绪度读取失败或缺少必要结果时必须 fail closed，界面将对应项标为“待确认”并提供重试，不得把未知状态当成 ready。
- 计划生成生命周期可见性 module：`app/plan_lifecycle.py` 继续拥有 `running`、`complete`、`failed`、`stale` 状态，并提供读取当前计划基准日最新生成记录的 interface；HTTP adapter 使用 `GET /api/plan/generated`。当前计划预览与生成生命周期是两种不同事实，页面不得用日期或本地状态猜测计划失效，也不得把旧计划动作当作当前建议继续执行。
- 计划生成并发边界：计划生成必须先占用 `running` 生命周期身份再读取账户输入；生成期间账户数据发生变化时，`running` 计划立即转为 `stale`，晚到的完成或失败结果只能从 `running` 条件更新，不能覆盖已经失效的状态。
- 账户写接口兼容边界：旧 snapshot/state 写端点只在账户尚未形成新模型当前版本时提供兼容；历史 backfill 不激活新模型当前状态。旧端点的兼容资格检查与 legacy 写入必须处于同一个串行化事务；某账户通过逐账户当前状态 interface 保存或确认后，旧写端点必须拒绝继续写入，并发首次激活也不得产生两套当前状态，避免账户页与计划生成分别读取不同真源。
- 计划生成动作：计划生成按钮属于计划入口，用于按当前冻结输入形成新版本。在准备状态中它是明确主操作；已有完整有效计划时，重新生成退为次级动作并说明会形成新版本，不得压过当前计划结论与执行正文。
- 阅读型内容：更新日志等以连续阅读为主的信息。它可以采用更舒展的编辑式排版，但仍属于投资决策工作台，服从同一套产品身份和体验语言。
- 更新日志统一边界：统一“方舟计划｜个人投资系统”的产品身份、全站导航、字体、颜色、间距和面向使用者的表达方式，但不把更新日志改造成工作台卡片流。正文保留按日期倒序的连续阅读形态以及日期、时间、类型、标题和正文五项既有可见字段；本轮不增加筛选、搜索或工程审计元数据。
- 更新日志信息流：更新日志继续以连续阅读为主，可以为每篇日志增加轻量影响范围标签，例如概览、计划、账户或视觉系统，用来帮助用户判断更新关联的工作流节点。影响范围标签只服务阅读定位，不把更新日志变成审计后台、issue 列表、功能矩阵或操作入口。
- 可视化门槛：图表默认不出现，只在能够回答明确决策问题时使用。本轮仅允许用紧凑的对比条或分配图辅助判断当前配置与目标配置的偏离，并与精确数值并列；事实就绪、行动顺序和订单检查继续使用状态、列表与表格。不为装饰常驻饼图或圆环图，也不把现有账户金额快照画成资产收益趋势；当前长期绩效真源仍在有知有行投资账本，ark-quant 尚不具备经外部资金流校正的绩效序列。
- 金额信息位置：概览只展示一次全体系总资产作为当前占比、目标占比和金额偏离的共同基数，不扩张为净值或收益看板；账户工作区展示各账户当前金额；计划只展示与计划可执行性直接相关的调拨金额、预计交易金额、计划后预计资金余额和账户资金前提。三类金额不得跨工作区重复堆叠。
- 模式、动效与个性化范围：本轮只设计浅色模式，不增加主题、密度、首页或导航个性化设置。动效仅服务于生成进度、展开收起、保存成功和错误提示等功能状态，不加入场、滚动、视差等装饰性效果；如实现非即时过渡，应尊重操作系统的减少动态效果偏好。
- 本轮信息架构原型：上一轮以“今日状态”为中枢的状态主线原型与选型结论已经被本轮自顶向下重设计取代，不再作为规格基线。本轮必须使用同一组真实内容制作三种结构差异明显的可丢弃全站原型，至少覆盖概览、计划和账户三个一级工作区，以及计划准备、需要执行、无需操作、失效或失败等关键状态；原型选型完成前不得把任何方向写成生产结论。
- 本轮原型验收方式：优先按真实任务走查而非审美投票，验证默认进入概览后能否理解 A/B/C 与 A 内部结构，能否在计划中选择是否同时进行组合间再平衡并只看到相关阻塞，能否按资金调拨后账户交易的顺序复述执行安排，能否从阻塞账户保存后无损返回计划，账户编辑是否保持各账户独立，以及历史、失效和失败计划是否不会被误执行。视觉比较在任务成立后进行。
- 工作台信息密度：采用层级明确的中等偏高密度。首屏优先呈现当前状态、异常和下一步动作，常用数据便于横向比较；计算依据、历史信息和高级操作渐进展开，避免内容站式过度留白和行情终端式数据堆积。
- 体验结构调整：Web 体验升级可以重排页面层级、区块顺序、标签和渐进展开方式，以改善判断与执行效率；投资规则、计算逻辑和数据口径不属于体验设计可改变的范围。
- 设备优先级：桌面端承担完整的账户维护、计划生成、依据核验和订单检查；手机端承担随身查看、计划状态确认、更新日志和少量轻操作，不要求在窄屏复刻桌面全部密集表格与高级操作。
- 视觉气质：采用克制的编辑感与精密的数据感。以浅色中性底、高对比正文、严谨网格、字体层级和细分割线建立秩序；蓝绿色作为系统强调色，红绿只承担真实业务语义，避免黑金理财风、渐变科技风、装饰性阴影和对外部产品的逐像素模仿。
- 视觉字体方向：Web 工作台采用“海图 + 账册”的视觉隐喻。大标题和日期标题使用克制的中文宋体骨架，表达账册和航海日志感；正文、导航和控件使用清晰系统黑体；金额、日期和代码使用等宽或表格数字字体。英文眉题只作为轻量坐标标记，不承担主要品牌或页面解释。
- 页面语义表达：页面标题、导航和关键区块标题应优先使用清晰中文承担信息架构。英文眉题可以作为轻量视觉节奏保留，但不承担解释页面职责，也不能替代中文语义。
