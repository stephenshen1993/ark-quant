# Web Dashboard 实施计划

> 状态：历史实施计划，不是当前实现真源。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建本机运行的个人投资管理 Web App，含账户/持仓历史留痕、榜单管理、交易计划生成四个页面。

**Architecture:** SQLite 存储所有历史数据；现有策略（CB/股票/rebalance）在原有出口处追加写库逻辑，不改主流程；FastAPI serve 静态前端，Alpine.js + Tailwind CDN 实现无构建工具的响应式页面。

**Tech Stack:** Python 3.9+, FastAPI, uvicorn, SQLite (stdlib sqlite3), pandas, Alpine.js 3, Tailwind CSS Play CDN

## Global Constraints

- Python 虚拟环境：`.venv/`，所有命令需先 `source .venv/bin/activate`
- 不改策略主流程逻辑，只在出口处追加写库调用
- DB 文件路径：`data/ark_quant.db`
- 测试框架：`python3 -m unittest discover tests/ -v`
- 不提交 `data/`, `outputs/`, `portfolios/` 下的数据文件
- 股票持仓：`stock_code` 列；转债持仓：`bond_code` 列，均为 6 位字符串（左补零）
- 策略 DataFrame 关键列名：CB ranked→`bond_code, bond_name, cb_price, premium_rate, double_low, score`；Stock ranked→`rank, stock_code, stock_name_q, total_mv_yuan, pe_ttm, roe_pct`；CB orders→`action, bond_code, bond_name, price, delta_shares, amount`；Stock orders→`action, stock_code, stock_name, price, delta_shares, amount`

---

## Task 1: 数据库层 `datasource/db.py`

**Files:**
- Create: `datasource/db.py`
- Create: `tests/test_db.py`

**Interfaces:**
- Produces:
  - `init_db() -> None`
  - `insert_strategy_run(strategy: str, data_date: date) -> int`
  - `insert_cb_rankings(run_id: int, df: pd.DataFrame) -> None`
  - `insert_stock_rankings(run_id: int, df: pd.DataFrame) -> None`
  - `insert_cb_orders(run_id: int, df: pd.DataFrame) -> None`
  - `insert_stock_orders(run_id: int, df: pd.DataFrame) -> None`
  - `insert_account_snapshot(snapshot_date, temperature, stock_total, stock_cash, bond_total, bond_cash, changqian_total, cash_pool, overseas_total) -> int`
  - `insert_positions(strategy: str, position_date: str, rows: list[dict]) -> None`
  - `get_latest_run_id(strategy: str) -> int | None`
  - `get_latest_account_snapshot() -> dict | None`
  - `get_account_history() -> list[dict]`
  - `get_latest_positions(strategy: str) -> list[dict]`
  - `get_position_dates(strategy: str) -> list[str]`
  - `get_positions_by_date(strategy: str, date_str: str) -> list[dict]`
  - `get_ranking_dates(strategy: str) -> list[str]`
  - `get_rankings(strategy: str, data_date: str) -> list[dict]`
  - `get_orders(strategy: str, data_date: str) -> list[dict]`
  - `get_latest_rankings(strategy: str) -> list[dict]`
  - `get_latest_orders(strategy: str) -> list[dict]`

- [ ] **Step 1: 写测试（先跑失败）**

```python
# tests/test_db.py
import unittest
from datetime import date
import pandas as pd
from datasource import db

class TestDb(unittest.TestCase):
    def setUp(self):
        # 用内存库隔离测试
        import sqlite3
        db._TEST_CONN = sqlite3.connect(":memory:")
        db._TEST_CONN.row_factory = sqlite3.Row
        db.init_db()

    def tearDown(self):
        db._TEST_CONN.close()
        db._TEST_CONN = None

    def test_insert_and_get_account_snapshot(self):
        db.insert_account_snapshot("2026-06-29", 45.0, 209555, 274, 227183, 110, 110606, 59013, 93030)
        snap = db.get_latest_account_snapshot()
        self.assertEqual(snap["snapshot_date"], "2026-06-29")
        self.assertAlmostEqual(snap["temperature"], 45.0)

    def test_insert_cb_rankings(self):
        run_id = db.insert_strategy_run("cb", date(2026, 6, 27))
        df = pd.DataFrame([
            {"bond_code": "113062", "bond_name": "常银转债", "cb_price": 127.74,
             "premium_rate": 13.29, "double_low": 141.03, "score": 0.824},
        ])
        db.insert_cb_rankings(run_id, df)
        rows = db.get_rankings("cb", "2026-06-27")
        self.assertEqual(len(rows), 1)
        self.assertEqual(rows[0]["bond_code"], "113062")
        self.assertEqual(rows[0]["rank"], 1)

    def test_insert_stock_rankings(self):
        run_id = db.insert_strategy_run("stock", date(2026, 6, 27))
        df = pd.DataFrame([
            {"rank": 1, "stock_code": "600455", "stock_name_q": "博通股份",
             "total_mv_yuan": 1.357e9, "pe_ttm": 29.76, "roe_pct": 13.83},
        ])
        db.insert_stock_rankings(run_id, df)
        rows = db.get_rankings("stock", "2026-06-27")
        self.assertEqual(rows[0]["stock_code"], "600455")
        self.assertAlmostEqual(rows[0]["market_cap"], 13.57, places=1)

    def test_insert_positions(self):
        db.insert_positions("cb", "2026-06-29", [
            {"code": "113062", "name": "常银转债", "shares": 90},
        ])
        rows = db.get_latest_positions("cb")
        self.assertEqual(rows[0]["code"], "113062")
        self.assertEqual(rows[0]["shares"], 90)

    def test_trade_date_is_next_weekday(self):
        run_id = db.insert_strategy_run("cb", date(2026, 6, 26))  # Friday
        rows = db.get_rankings("cb", "2026-06-26")  # empty but run exists
        with db.get_connection() as conn:
            r = conn.execute("SELECT trade_date FROM strategy_runs WHERE id=?", (run_id,)).fetchone()
        self.assertEqual(r["trade_date"], "2026-06-29")  # Monday

    def test_account_history(self):
        db.insert_account_snapshot("2026-06-22", 52.0, 200000, 300, 220000, 200, 105000, 60000, 90000)
        db.insert_account_snapshot("2026-06-29", 45.0, 209555, 274, 227183, 110, 110606, 59013, 93030)
        history = db.get_account_history()
        self.assertEqual(len(history), 2)
        self.assertEqual(history[0]["snapshot_date"], "2026-06-22")
```

- [ ] **Step 2: 确认测试失败**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_db -v 2>&1 | head -20
```
期望：`ImportError` 或 `ModuleNotFoundError`（db.py 不存在）

- [ ] **Step 3: 实现 `datasource/db.py`**

```python
# datasource/db.py
from __future__ import annotations

import sqlite3
from datetime import date, datetime, timedelta
from pathlib import Path
from typing import Any

import pandas as pd

DB_PATH = Path(__file__).resolve().parents[1] / "data" / "ark_quant.db"

_TEST_CONN: sqlite3.Connection | None = None  # 测试注入点


def get_connection() -> sqlite3.Connection:
    if _TEST_CONN is not None:
        return _TEST_CONN
    conn = sqlite3.connect(DB_PATH)
    conn.row_factory = sqlite3.Row
    return conn


def init_db() -> None:
    with get_connection() as conn:
        conn.executescript("""
        PRAGMA foreign_keys = ON;

        CREATE TABLE IF NOT EXISTS strategy_runs (
            id          INTEGER PRIMARY KEY AUTOINCREMENT,
            strategy    TEXT    NOT NULL,
            data_date   TEXT    NOT NULL,
            trade_date  TEXT,
            created_at  TEXT    NOT NULL
        );

        CREATE TABLE IF NOT EXISTS cb_rankings (
            id           INTEGER PRIMARY KEY AUTOINCREMENT,
            run_id       INTEGER NOT NULL REFERENCES strategy_runs(id),
            rank         INTEGER NOT NULL,
            bond_code    TEXT    NOT NULL,
            bond_name    TEXT,
            cb_price     REAL,
            premium_rate REAL,
            double_low   REAL,
            score        REAL
        );

        CREATE TABLE IF NOT EXISTS stock_rankings (
            id         INTEGER PRIMARY KEY AUTOINCREMENT,
            run_id     INTEGER NOT NULL REFERENCES strategy_runs(id),
            rank       INTEGER NOT NULL,
            stock_code TEXT    NOT NULL,
            stock_name TEXT,
            market_cap REAL,
            pe_ttm     REAL,
            roe_ex     REAL
        );

        CREATE TABLE IF NOT EXISTS cb_orders (
            id        INTEGER PRIMARY KEY AUTOINCREMENT,
            run_id    INTEGER NOT NULL REFERENCES strategy_runs(id),
            action    TEXT    NOT NULL,
            bond_code TEXT    NOT NULL,
            bond_name TEXT,
            price     REAL,
            shares    INTEGER,
            amount    REAL
        );

        CREATE TABLE IF NOT EXISTS stock_orders (
            id         INTEGER PRIMARY KEY AUTOINCREMENT,
            run_id     INTEGER NOT NULL REFERENCES strategy_runs(id),
            action     TEXT    NOT NULL,
            stock_code TEXT    NOT NULL,
            stock_name TEXT,
            price      REAL,
            shares     INTEGER,
            amount     REAL
        );

        CREATE TABLE IF NOT EXISTS account_snapshots (
            id              INTEGER PRIMARY KEY AUTOINCREMENT,
            snapshot_date   TEXT    NOT NULL,
            temperature     REAL,
            stock_total     REAL,
            stock_cash      REAL,
            bond_total      REAL,
            bond_cash       REAL,
            changqian_total REAL,
            cash_pool       REAL,
            overseas_total  REAL,
            created_at      TEXT    NOT NULL
        );

        CREATE TABLE IF NOT EXISTS cb_positions (
            id            INTEGER PRIMARY KEY AUTOINCREMENT,
            position_date TEXT    NOT NULL,
            bond_code     TEXT    NOT NULL,
            bond_name     TEXT,
            shares        INTEGER NOT NULL
        );

        CREATE TABLE IF NOT EXISTS stock_positions (
            id            INTEGER PRIMARY KEY AUTOINCREMENT,
            position_date TEXT    NOT NULL,
            stock_code    TEXT    NOT NULL,
            stock_name    TEXT,
            shares        INTEGER NOT NULL
        );
        """)


def _next_weekday(d: date) -> date:
    d = d + timedelta(days=1)
    while d.weekday() >= 5:
        d += timedelta(days=1)
    return d


# ── Write ─────────────────────────────────────────────────────────────────────

def insert_strategy_run(strategy: str, data_date: date) -> int:
    trade_date = _next_weekday(data_date)
    with get_connection() as conn:
        cur = conn.execute(
            "INSERT INTO strategy_runs (strategy, data_date, trade_date, created_at) VALUES (?,?,?,?)",
            (strategy, data_date.isoformat(), trade_date.isoformat(), datetime.now().isoformat()),
        )
        return cur.lastrowid


def insert_cb_rankings(run_id: int, df: pd.DataFrame) -> None:
    rows = [
        (run_id, i, str(getattr(r, "bond_code", "")).zfill(6),
         getattr(r, "bond_name", ""), getattr(r, "cb_price", None),
         getattr(r, "premium_rate", None), getattr(r, "double_low", None),
         getattr(r, "score", None))
        for i, r in enumerate(df.itertuples(index=False), start=1)
    ]
    with get_connection() as conn:
        conn.executemany(
            "INSERT INTO cb_rankings (run_id,rank,bond_code,bond_name,cb_price,premium_rate,double_low,score) VALUES (?,?,?,?,?,?,?,?)",
            rows,
        )


def insert_stock_rankings(run_id: int, df: pd.DataFrame) -> None:
    rows = [
        (run_id, int(getattr(r, "rank", i)),
         str(getattr(r, "stock_code", "")).zfill(6),
         getattr(r, "stock_name_q", getattr(r, "stock_name", "")),
         getattr(r, "total_mv_yuan", None) / 1e8 if getattr(r, "total_mv_yuan", None) is not None else None,
         getattr(r, "pe_ttm", None), getattr(r, "roe_pct", None))
        for i, r in enumerate(df.itertuples(index=False), start=1)
    ]
    with get_connection() as conn:
        conn.executemany(
            "INSERT INTO stock_rankings (run_id,rank,stock_code,stock_name,market_cap,pe_ttm,roe_ex) VALUES (?,?,?,?,?,?,?)",
            rows,
        )


def insert_cb_orders(run_id: int, df: pd.DataFrame) -> None:
    rows = [
        (run_id, r.action, str(r.bond_code).zfill(6),
         getattr(r, "bond_name", ""), r.price, int(r.delta_shares), r.amount)
        for r in df.itertuples(index=False)
    ]
    with get_connection() as conn:
        conn.executemany(
            "INSERT INTO cb_orders (run_id,action,bond_code,bond_name,price,shares,amount) VALUES (?,?,?,?,?,?,?)",
            rows,
        )


def insert_stock_orders(run_id: int, df: pd.DataFrame) -> None:
    rows = [
        (run_id, r.action, str(r.stock_code).zfill(6),
         getattr(r, "stock_name", ""), r.price, int(r.delta_shares), r.amount)
        for r in df.itertuples(index=False)
    ]
    with get_connection() as conn:
        conn.executemany(
            "INSERT INTO stock_orders (run_id,action,stock_code,stock_name,price,shares,amount) VALUES (?,?,?,?,?,?,?)",
            rows,
        )


def insert_account_snapshot(
    snapshot_date: str, temperature: float,
    stock_total: float, stock_cash: float,
    bond_total: float, bond_cash: float,
    changqian_total: float, cash_pool: float, overseas_total: float,
) -> int:
    with get_connection() as conn:
        cur = conn.execute(
            """INSERT INTO account_snapshots
               (snapshot_date,temperature,stock_total,stock_cash,bond_total,bond_cash,
                changqian_total,cash_pool,overseas_total,created_at)
               VALUES (?,?,?,?,?,?,?,?,?,?)""",
            (snapshot_date, temperature, stock_total, stock_cash, bond_total, bond_cash,
             changqian_total, cash_pool, overseas_total, datetime.now().isoformat()),
        )
        return cur.lastrowid


def insert_positions(strategy: str, position_date: str, rows: list[dict]) -> None:
    if strategy == "cb":
        table, code_col, name_col = "cb_positions", "bond_code", "bond_name"
    else:
        table, code_col, name_col = "stock_positions", "stock_code", "stock_name"
    with get_connection() as conn:
        conn.execute(f"DELETE FROM {table} WHERE position_date=?", (position_date,))
        conn.executemany(
            f"INSERT INTO {table} (position_date,{code_col},{name_col},shares) VALUES (?,?,?,?)",
            [(position_date, r["code"], r.get("name", ""), r["shares"]) for r in rows],
        )


# ── Read ──────────────────────────────────────────────────────────────────────

def get_latest_run_id(strategy: str) -> int | None:
    with get_connection() as conn:
        row = conn.execute(
            "SELECT id FROM strategy_runs WHERE strategy=? ORDER BY id DESC LIMIT 1",
            (strategy,)
        ).fetchone()
        return row["id"] if row else None


def get_latest_account_snapshot() -> dict | None:
    with get_connection() as conn:
        row = conn.execute(
            "SELECT * FROM account_snapshots ORDER BY id DESC LIMIT 1"
        ).fetchone()
        return dict(row) if row else None


def get_account_history() -> list[dict]:
    with get_connection() as conn:
        rows = conn.execute(
            "SELECT snapshot_date, stock_total, bond_total, changqian_total, cash_pool, overseas_total "
            "FROM account_snapshots ORDER BY snapshot_date"
        ).fetchall()
        return [dict(r) for r in rows]


def get_latest_positions(strategy: str) -> list[dict]:
    if strategy == "cb":
        table, code_col, name_col = "cb_positions", "bond_code", "bond_name"
    else:
        table, code_col, name_col = "stock_positions", "stock_code", "stock_name"
    with get_connection() as conn:
        row = conn.execute(f"SELECT MAX(position_date) as d FROM {table}").fetchone()
        if not row or not row["d"]:
            return []
        rows = conn.execute(
            f"SELECT {code_col} as code, {name_col} as name, shares, position_date "
            f"FROM {table} WHERE position_date=? ORDER BY {code_col}",
            (row["d"],)
        ).fetchall()
        return [dict(r) for r in rows]


def get_position_dates(strategy: str) -> list[str]:
    table = "cb_positions" if strategy == "cb" else "stock_positions"
    with get_connection() as conn:
        rows = conn.execute(
            f"SELECT DISTINCT position_date FROM {table} ORDER BY position_date DESC"
        ).fetchall()
        return [r["position_date"] for r in rows]


def get_positions_by_date(strategy: str, date_str: str) -> list[dict]:
    if strategy == "cb":
        table, code_col, name_col = "cb_positions", "bond_code", "bond_name"
    else:
        table, code_col, name_col = "stock_positions", "stock_code", "stock_name"
    with get_connection() as conn:
        rows = conn.execute(
            f"SELECT {code_col} as code, {name_col} as name, shares "
            f"FROM {table} WHERE position_date=? ORDER BY {code_col}",
            (date_str,)
        ).fetchall()
        return [dict(r) for r in rows]


def get_ranking_dates(strategy: str) -> list[str]:
    with get_connection() as conn:
        rows = conn.execute(
            "SELECT DISTINCT data_date FROM strategy_runs WHERE strategy=? ORDER BY data_date DESC",
            (strategy,)
        ).fetchall()
        return [r["data_date"] for r in rows]


def get_rankings(strategy: str, data_date: str) -> list[dict]:
    table = "cb_rankings" if strategy == "cb" else "stock_rankings"
    with get_connection() as conn:
        run = conn.execute(
            "SELECT id, trade_date FROM strategy_runs WHERE strategy=? AND data_date=? ORDER BY id DESC LIMIT 1",
            (strategy, data_date),
        ).fetchone()
        if not run:
            return []
        rows = conn.execute(
            f"SELECT * FROM {table} WHERE run_id=? ORDER BY rank", (run["id"],)
        ).fetchall()
        result = [dict(r) for r in rows]
        for r in result:
            r["trade_date"] = run["trade_date"]
        return result


def get_orders(strategy: str, data_date: str) -> list[dict]:
    table = "cb_orders" if strategy == "cb" else "stock_orders"
    with get_connection() as conn:
        run = conn.execute(
            "SELECT id FROM strategy_runs WHERE strategy=? AND data_date=? ORDER BY id DESC LIMIT 1",
            (strategy, data_date),
        ).fetchone()
        if not run:
            return []
        rows = conn.execute(
            f"SELECT * FROM {table} WHERE run_id=? ORDER BY id", (run["id"],)
        ).fetchall()
        return [dict(r) for r in rows]


def get_latest_rankings(strategy: str) -> list[dict]:
    dates = get_ranking_dates(strategy)
    return get_rankings(strategy, dates[0]) if dates else []


def get_latest_orders(strategy: str) -> list[dict]:
    dates = get_ranking_dates(strategy)
    return get_orders(strategy, dates[0]) if dates else []
```

- [ ] **Step 4: 跑测试，确认全通**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_db -v
```
期望：6 tests passed

- [ ] **Step 5: commit**

```bash
git add datasource/db.py tests/test_db.py
git commit -m "feat: add SQLite db layer (datasource/db.py)"
```

---

## Task 2: CB 策略写库

**Files:**
- Modify: `strategies/cb_rotation/run.py` — `run()` 函数末尾追加写库
- Modify: `strategies/cb_rotation/size_orders.py` — `main()` 末尾追加写库
- Create: `tests/test_cb_db_write.py`

**Interfaces:**
- Consumes: `datasource.db.insert_strategy_run`, `insert_cb_rankings`, `insert_cb_orders`, `get_latest_run_id`, `init_db`

- [ ] **Step 1: 写集成测试**

```python
# tests/test_cb_db_write.py
import unittest
from pathlib import Path
from unittest.mock import patch, MagicMock
import pandas as pd
from datasource import db

class TestCbDbWrite(unittest.TestCase):
    def setUp(self):
        import sqlite3
        db._TEST_CONN = sqlite3.connect(":memory:")
        db._TEST_CONN.row_factory = sqlite3.Row
        db.init_db()

    def tearDown(self):
        db._TEST_CONN.close()
        db._TEST_CONN = None

    def test_save_outputs_writes_rankings(self):
        from strategies.cb_rotation.run import save_outputs
        target = pd.DataFrame([{
            "bond_code": "113062", "bond_name": "常银转债",
            "cb_price": 127.74, "premium_rate": 13.29,
            "double_low": 141.03, "score": 0.824, "target_weight": 0.05,
        }])
        rebalance = pd.DataFrame(columns=["action", "bond_code", "bond_name", "target_weight"])
        config = {"strategy_name": "Test", "top_n": 20}
        log_file = Path("/tmp/test.log")
        from datetime import date
        save_outputs(target, rebalance, config, log_file, [], data_date=date(2026, 6, 27))
        rows = db.get_rankings("cb", "2026-06-27")
        self.assertEqual(len(rows), 1)
        self.assertEqual(rows[0]["bond_code"], "113062")
```

- [ ] **Step 2: 跑测试确认失败**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_cb_db_write -v 2>&1 | head -15
```
期望：`TypeError`（save_outputs 不接受 data_date 参数）

- [ ] **Step 3: 修改 `strategies/cb_rotation/run.py` 的 `save_outputs` 和 `run`**

在 `save_outputs` 函数签名末尾加 `data_date: date | None = None` 参数，并在函数末尾追加写库：

```python
# 在 save_outputs() 函数签名中加 data_date 参数
def save_outputs(
    target: pd.DataFrame,
    rebalance: pd.DataFrame,
    config: dict,
    log_file: Path,
    data_notes: list[str],
    data_date: "date | None" = None,  # 新增
) -> RunArtifacts:
    # ... 原有代码不变 ...

    # 在 return RunArtifacts(...) 之前追加:
    if data_date is not None:
        try:
            from datasource.db import init_db, insert_strategy_run, insert_cb_rankings
            init_db()
            run_id = insert_strategy_run("cb", data_date)
            insert_cb_rankings(run_id, target)
        except Exception as exc:
            logging.warning("DB write failed (cb rankings): %s", exc)

    return RunArtifacts(candidates_csv, candidates_xlsx, rebalance_csv, report_md)
```

在 `run()` 函数中，调用 `save_outputs` 时传入 `data_date`：

```python
# run() 函数中，原来的 artifacts = save_outputs(target, rebalance, config, log_file, data_notes)
# 改为：
artifacts = save_outputs(
    target, rebalance, config, log_file, data_notes,
    data_date=resolve_trade_date(scored),
)
```

- [ ] **Step 4: 修改 `strategies/cb_rotation/size_orders.py` 的 `main()`**

在 `print(f"下单清单已存: {out}")` 之后追加：

```python
    try:
        from datasource.db import init_db, get_latest_run_id, insert_cb_orders
        init_db()
        run_id = get_latest_run_id("cb")
        if run_id is not None:
            insert_cb_orders(run_id, sheet)
    except Exception as exc:
        logging.warning("DB write failed (cb orders): %s", exc)
```

- [ ] **Step 5: 跑测试**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_cb_db_write -v
```
期望：1 test passed

- [ ] **Step 6: 冒烟测试（确认策略跑完能查到库里的数据）**

```bash
source .venv/bin/activate
python3 -m strategies.cb_rotation.run --max-bonds 30 2>&1 | tail -5
python3 -c "from datasource import db; db.init_db(); print(db.get_ranking_dates('cb'))"
```
期望：打印出今日日期

- [ ] **Step 7: commit**

```bash
git add strategies/cb_rotation/run.py strategies/cb_rotation/size_orders.py tests/test_cb_db_write.py
git commit -m "feat: cb strategy writes rankings and orders to SQLite"
```

---

## Task 3: Stock 策略写库

**Files:**
- Modify: `strategies/stock_smallcap/run.py` — `save_outputs()` 追加写库，`run()` 传 data_date
- Modify: `strategies/stock_smallcap/size_orders.py` — `main()` 末尾追加写库
- Create: `tests/test_stock_db_write.py`

**Interfaces:**
- Consumes: `datasource.db.insert_strategy_run`, `insert_stock_rankings`, `insert_stock_orders`, `get_latest_run_id`, `init_db`
- Note: stock `ranked` DataFrame 的 data_date 用 `date.today()`（股票策略无历史数据基准日）

- [ ] **Step 1: 写测试**

```python
# tests/test_stock_db_write.py
import unittest
import sqlite3
import pandas as pd
from pathlib import Path
from datasource import db

class TestStockDbWrite(unittest.TestCase):
    def setUp(self):
        db._TEST_CONN = sqlite3.connect(":memory:")
        db._TEST_CONN.row_factory = sqlite3.Row
        db.init_db()

    def tearDown(self):
        db._TEST_CONN.close()
        db._TEST_CONN = None

    def test_save_outputs_writes_rankings(self):
        from strategies.stock_smallcap.run import save_outputs
        from datetime import date
        ranked = pd.DataFrame([{
            "rank": 1, "stock_code": "600455", "stock_name_q": "博通股份",
            "total_mv_yuan": 1.357e9, "amount_yuan": 3.3e7,
            "pe_ttm": 29.76, "roe_pct": 13.83, "price": 21.72,
        }])
        rebalance = pd.DataFrame(columns=["action", "stock_code", "stock_name", "rank", "note"])
        config = {
            "strategy_name": "Test", "selection": {"hold_n": 20, "sell_rank": 21, "candidate_pool": 25},
            "filters": {"min_amount_yuan": 1e7, "min_roe_pct": -1.0},
        }
        save_outputs(ranked, rebalance, config, Path("/tmp/t.log"), [], data_date=date(2026, 6, 27))
        rows = db.get_rankings("stock", "2026-06-27")
        self.assertEqual(len(rows), 1)
        self.assertEqual(rows[0]["stock_code"], "600455")
        self.assertAlmostEqual(rows[0]["market_cap"], 13.57, places=1)
```

- [ ] **Step 2: 跑测试确认失败**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_stock_db_write -v 2>&1 | head -10
```

- [ ] **Step 3: 修改 `strategies/stock_smallcap/run.py` 的 `save_outputs` 和 `run`**

`save_outputs` 签名加 `data_date: "date | None" = None`，在 `return RunArtifacts(...)` 前追加：

```python
    if data_date is not None:
        try:
            from datasource.db import init_db, insert_strategy_run, insert_stock_rankings
            init_db()
            run_id = insert_strategy_run("stock", data_date)
            insert_stock_rankings(run_id, ranked)
        except Exception as exc:
            logging.warning("DB write failed (stock rankings): %s", exc)
```

在 `run()` 中（`save_outputs` 调用处）传入 `data_date=date.today()`：

```python
from datetime import date as _date
# ...
artifacts = save_outputs(ranked, rebalance, config, log_file, notes, data_date=_date.today())
```

- [ ] **Step 4: 修改 `strategies/stock_smallcap/size_orders.py` 的 `main()`**

在 `print(f"下单清单已存: {out}")` 后追加：

```python
    try:
        from datasource.db import init_db, get_latest_run_id, insert_stock_orders
        init_db()
        run_id = get_latest_run_id("stock")
        if run_id is not None:
            insert_stock_orders(run_id, sheet)
    except Exception as exc:
        import logging as _log
        _log.warning("DB write failed (stock orders): %s", exc)
```

- [ ] **Step 5: 跑测试**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_stock_db_write -v
```

- [ ] **Step 6: commit**

```bash
git add strategies/stock_smallcap/run.py strategies/stock_smallcap/size_orders.py tests/test_stock_db_write.py
git commit -m "feat: stock strategy writes rankings and orders to SQLite"
```

---

## Task 4: rebalance.py 写库

**Files:**
- Modify: `rebalance.py` — `main()` 末尾追加写库

**Interfaces:**
- Consumes: `datasource.db.insert_account_snapshot`, `init_db`
- Note: 只在 `not args.no_save` 分支写库（与 save_state 保持一致）

- [ ] **Step 1: 修改 `rebalance.py` 的 `main()`**

在 `if not args.no_save:` 块中，`save_state(state)` 之后追加：

```python
    if not args.no_save:
        save_state(state)
        print(f"  (已保存至 {STATE_FILE.relative_to(Path(__file__).parent)})")
        # 新增：写入 SQLite
        try:
            from datasource.db import init_db, insert_account_snapshot
            init_db()
            insert_account_snapshot(
                snapshot_date=state["updated_at"],
                temperature=state["temperature"],
                stock_total=state["stock"]["total"],
                stock_cash=state["stock"].get("cash", 0.0),
                bond_total=state["bond"]["total"],
                bond_cash=state["bond"].get("cash", 0.0),
                changqian_total=state["changqian"]["total"],
                cash_pool=state["cash_pool"]["total"],
                overseas_total=state["overseas"]["total"],
            )
        except Exception as exc:
            print(f"  (DB 写入失败: {exc})")
```

- [ ] **Step 2: 验证**

```bash
source .venv/bin/activate
python3 rebalance.py --temperature 45
python3 -c "from datasource import db; db.init_db(); snap = db.get_latest_account_snapshot(); print(snap['snapshot_date'], snap['temperature'])"
```
期望：打印今日日期和 45.0

- [ ] **Step 3: commit**

```bash
git add rebalance.py
git commit -m "feat: rebalance.py writes account snapshot to SQLite"
```

---

## Task 5: FastAPI 骨架

**Files:**
- Modify: `requirements.txt` — 追加 fastapi, uvicorn
- Create: `app/__init__.py`
- Create: `app/main.py`
- Create: `app/routers/__init__.py`
- Create: `app/static/` 目录（占位）
- Create: `run_app.py`

**Interfaces:**
- Produces: `GET /health` → `{"status": "ok"}`；`app.app` FastAPI 实例

- [ ] **Step 1: 更新 requirements.txt**

在文件末尾追加：

```
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
```

安装：

```bash
source .venv/bin/activate && pip install fastapi "uvicorn[standard]" 2>&1 | tail -5
```

- [ ] **Step 2: 创建 `app/__init__.py` 和 `app/routers/__init__.py`**

两个文件均为空文件：

```bash
touch app/__init__.py app/routers/__init__.py
mkdir -p app/static
```

- [ ] **Step 3: 创建 `app/main.py`**

```python
# app/main.py
from pathlib import Path

from fastapi import FastAPI
from fastapi.staticfiles import StaticFiles

from datasource.db import init_db

app = FastAPI(title="ark-quant dashboard")

init_db()

STATIC_DIR = Path(__file__).parent / "static"
STATIC_DIR.mkdir(exist_ok=True)
app.mount("/static", StaticFiles(directory=STATIC_DIR), name="static")


@app.get("/health")
def health():
    return {"status": "ok"}
```

- [ ] **Step 4: 创建 `run_app.py`**

```python
# run_app.py
import uvicorn

if __name__ == "__main__":
    uvicorn.run("app.main:app", host="127.0.0.1", port=8000, reload=True)
```

- [ ] **Step 5: 验证**

```bash
source .venv/bin/activate
python3 run_app.py &
sleep 2
curl http://127.0.0.1:8000/health
kill %1
```
期望：`{"status":"ok"}`

- [ ] **Step 6: commit**

```bash
git add requirements.txt app/__init__.py app/main.py app/routers/__init__.py run_app.py
git commit -m "feat: FastAPI app skeleton with health check"
```

---

## Task 6: 账户总览 API + 页面

**Files:**
- Create: `app/routers/accounts.py`
- Create: `app/routers/positions.py`
- Modify: `app/main.py` — include routers
- Create: `app/static/index.html`
- Create: `tests/test_api_accounts.py`

**Interfaces:**
- Produces:
  - `GET /api/account/latest` → `AccountSnapshot | null`
  - `GET /api/account/history` → `list[HistoryPoint]`
  - `POST /api/account/snapshot` body: `AccountSnapshotIn`
  - `GET /api/positions/{strategy}` → `list[Position]`
  - `GET /api/positions/{strategy}/dates` → `list[str]`
  - `POST /api/positions/{strategy}` body: `list[PositionIn]`

- [ ] **Step 1: 写 API 测试**

```python
# tests/test_api_accounts.py
import unittest
import sqlite3
from fastapi.testclient import TestClient
from datasource import db

class TestAccountsApi(unittest.TestCase):
    def setUp(self):
        db._TEST_CONN = sqlite3.connect(":memory:")
        db._TEST_CONN.row_factory = sqlite3.Row
        db.init_db()
        from app.main import app
        self.client = TestClient(app)

    def tearDown(self):
        db._TEST_CONN.close()
        db._TEST_CONN = None

    def test_get_latest_returns_null_when_empty(self):
        r = self.client.get("/api/account/latest")
        self.assertEqual(r.status_code, 200)
        self.assertIsNone(r.json())

    def test_post_and_get_snapshot(self):
        payload = {
            "snapshot_date": "2026-06-29", "temperature": 45.0,
            "stock_total": 209555, "stock_cash": 274,
            "bond_total": 227183, "bond_cash": 110,
            "changqian_total": 110606, "cash_pool": 59013, "overseas_total": 93030,
        }
        r = self.client.post("/api/account/snapshot", json=payload)
        self.assertEqual(r.status_code, 200)
        r2 = self.client.get("/api/account/latest")
        self.assertEqual(r2.json()["temperature"], 45.0)

    def test_post_and_get_positions(self):
        payload = [{"code": "113062", "name": "常银转债", "shares": 90}]
        r = self.client.post("/api/positions/cb?position_date=2026-06-29", json=payload)
        self.assertEqual(r.status_code, 200)
        r2 = self.client.get("/api/positions/cb")
        self.assertEqual(r2.json()[0]["code"], "113062")
```

- [ ] **Step 2: 确认测试失败**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_accounts -v 2>&1 | head -15
```

- [ ] **Step 3: 创建 `app/routers/accounts.py`**

```python
# app/routers/accounts.py
from __future__ import annotations
from datetime import date
from typing import Optional
from fastapi import APIRouter
from pydantic import BaseModel
from datasource import db

router = APIRouter(prefix="/api/account", tags=["account"])


class AccountSnapshotIn(BaseModel):
    snapshot_date: str
    temperature: float
    stock_total: float
    stock_cash: float
    bond_total: float
    bond_cash: float
    changqian_total: float
    cash_pool: float
    overseas_total: float


@router.get("/latest")
def get_latest():
    return db.get_latest_account_snapshot()


@router.get("/history")
def get_history():
    return db.get_account_history()


@router.post("/snapshot")
def post_snapshot(body: AccountSnapshotIn):
    db.insert_account_snapshot(**body.model_dump())
    return {"ok": True}
```

- [ ] **Step 4: 创建 `app/routers/positions.py`**

```python
# app/routers/positions.py
from __future__ import annotations
from typing import Literal
from fastapi import APIRouter
from pydantic import BaseModel
from datasource import db

router = APIRouter(prefix="/api/positions", tags=["positions"])


class PositionIn(BaseModel):
    code: str
    name: str = ""
    shares: int


@router.get("/{strategy}")
def get_positions(strategy: Literal["cb", "stock"], date: str | None = None):
    if date:
        return db.get_positions_by_date(strategy, date)
    return db.get_latest_positions(strategy)


@router.get("/{strategy}/dates")
def get_dates(strategy: Literal["cb", "stock"]):
    return db.get_position_dates(strategy)


@router.post("/{strategy}")
def post_positions(strategy: Literal["cb", "stock"], body: list[PositionIn], position_date: str):
    db.insert_positions(strategy, position_date, [p.model_dump() for p in body])
    return {"ok": True}
```

- [ ] **Step 5: 更新 `app/main.py` 注册路由**

```python
# app/main.py — 在 health 路由之前追加:
from app.routers import accounts, positions
app.include_router(accounts.router)
app.include_router(positions.router)
```

- [ ] **Step 6: 跑 API 测试**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_accounts -v
```
期望：3 tests passed

- [ ] **Step 7: 创建 `app/static/index.html`（账户总览 + 持仓页）**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>ark-quant</title>
<script src="https://cdn.tailwindcss.com"></script>
<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3/dist/cdn.min.js"></script>
</head>
<body class="bg-gray-50 text-gray-900 flex h-screen">

<!-- 侧边导航 -->
<nav class="w-40 bg-white border-r border-gray-200 flex flex-col pt-6 px-3 gap-1 shrink-0">
  <span class="text-xs text-gray-400 font-medium px-2 mb-2">ark-quant</span>
  <template x-data x-for="item in [
    {id:'account', label:'账户总览', icon:'📊'},
    {id:'positions', label:'持仓', icon:'💼'},
    {id:'plan', label:'本期计划', icon:'📋'},
    {id:'rankings', label:'榜单', icon:'🏆'}
  ]">
    <button @click="$dispatch('nav', item.id)"
      :class="$store.page === item.id
        ? 'bg-gray-100 text-gray-900 font-medium'
        : 'text-gray-500 hover:bg-gray-50'"
      class="flex items-center gap-2 px-3 py-2 rounded-lg text-sm text-left w-full">
      <span x-text="item.icon"></span>
      <span x-text="item.label"></span>
    </button>
  </template>
</nav>

<!-- 主内容 -->
<main class="flex-1 overflow-auto p-6" x-data @nav.window="$store.page = $event.detail">

  <!-- 账户总览 -->
  <div x-show="$store.page === 'account'" x-data="accountPage()" x-init="load()">
    <div class="flex justify-between items-center mb-4">
      <h1 class="text-lg font-medium">账户总览</h1>
      <span x-show="snap" class="text-xs text-gray-400"
        x-text="snap ? snap.snapshot_date + ' · 温度 ' + snap.temperature + '°' : ''"></span>
    </div>
    <!-- 账户卡片 -->
    <div class="grid grid-cols-4 gap-3 mb-4" x-show="snap">
      <div class="bg-white border border-gray-100 rounded-xl p-4">
        <div class="text-xs text-gray-400 mb-1">股票账户</div>
        <div class="text-lg font-medium" x-text="fmt(snap?.stock_total)"></div>
        <div class="text-xs text-gray-400 mt-1" x-text="'现金 ' + fmt(snap?.stock_cash)"></div>
      </div>
      <div class="bg-white border border-gray-100 rounded-xl p-4">
        <div class="text-xs text-gray-400 mb-1">转债账户</div>
        <div class="text-lg font-medium" x-text="fmt(snap?.bond_total)"></div>
        <div class="text-xs text-gray-400 mt-1" x-text="'现金 ' + fmt(snap?.bond_cash)"></div>
      </div>
      <div class="bg-white border border-gray-100 rounded-xl p-4">
        <div class="text-xs text-gray-400 mb-1">长钱账户</div>
        <div class="text-lg font-medium" x-text="fmt(snap?.changqian_total)"></div>
      </div>
      <div class="bg-white border border-gray-100 rounded-xl p-4">
        <div class="text-xs text-gray-400 mb-1">现金池</div>
        <div class="text-lg font-medium" x-text="fmt(snap?.cash_pool)"></div>
      </div>
    </div>
    <div x-show="!snap" class="text-sm text-gray-400 mb-4">暂无账户数据，请录入。</div>

    <!-- 录入表单 -->
    <div class="bg-white border border-gray-100 rounded-xl p-4 mb-4">
      <div class="text-sm font-medium mb-3">更新账户数据</div>
      <div class="grid grid-cols-3 gap-3 text-sm">
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">快照日期</span>
          <input type="date" x-model="form.snapshot_date" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">温度</span>
          <input type="number" x-model.number="form.temperature" class="border border-gray-200 rounded px-2 py-1" placeholder="45">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">股票总市值</span>
          <input type="number" x-model.number="form.stock_total" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">股票现金</span>
          <input type="number" x-model.number="form.stock_cash" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">转债总市值</span>
          <input type="number" x-model.number="form.bond_total" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">转债现金</span>
          <input type="number" x-model.number="form.bond_cash" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">长钱净值</span>
          <input type="number" x-model.number="form.changqian_total" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">现金池余额</span>
          <input type="number" x-model.number="form.cash_pool" class="border border-gray-200 rounded px-2 py-1">
        </label>
        <label class="flex flex-col gap-1">
          <span class="text-xs text-gray-400">海外长钱净值</span>
          <input type="number" x-model.number="form.overseas_total" class="border border-gray-200 rounded px-2 py-1">
        </label>
      </div>
      <button @click="save()" class="mt-3 px-4 py-1.5 bg-gray-900 text-white text-sm rounded-lg hover:bg-gray-700">
        保存
      </button>
      <span x-show="saved" class="ml-3 text-xs text-green-600">已保存</span>
    </div>
  </div>

  <!-- 持仓页 -->
  <div x-show="$store.page === 'positions'" x-data="positionsPage()" x-init="load()">
    <div class="flex justify-between items-center mb-4">
      <h1 class="text-lg font-medium">持仓</h1>
      <div class="flex gap-2">
        <select x-model="dateFilter" @change="loadByDate()" class="text-xs border border-gray-200 rounded px-2 py-1">
          <option value="">最新</option>
          <template x-for="d in dates"><option :value="d" x-text="d"></option></template>
        </select>
      </div>
    </div>
    <div class="flex gap-2 mb-4">
      <button @click="tab='cb'" :class="tab==='cb'?'bg-gray-900 text-white':'bg-white text-gray-500 border border-gray-200'"
        class="px-4 py-1.5 rounded-lg text-sm">转债持仓</button>
      <button @click="tab='stock'" :class="tab==='stock'?'bg-gray-900 text-white':'bg-white text-gray-500 border border-gray-200'"
        class="px-4 py-1.5 rounded-lg text-sm">股票持仓</button>
    </div>
    <!-- 持仓表格 -->
    <div class="bg-white border border-gray-100 rounded-xl overflow-hidden mb-4">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 text-gray-400 text-xs">
          <tr>
            <th class="text-left px-4 py-2">代码</th>
            <th class="text-left px-4 py-2">名称</th>
            <th class="text-right px-4 py-2" x-text="tab==='cb'?'持有(张)':'持有(股)'"></th>
            <th class="w-8"></th>
          </tr>
        </thead>
        <tbody>
          <template x-for="(row, idx) in currentRows()" :key="idx">
            <tr class="border-t border-gray-50">
              <td class="px-4 py-2" x-text="row.code"></td>
              <td class="px-4 py-2 text-gray-600" x-text="row.name"></td>
              <td class="px-4 py-2 text-right">
                <input type="number" x-model.number="row.shares"
                  class="w-20 text-right border border-gray-100 rounded px-1 py-0.5 text-sm">
              </td>
              <td class="px-2">
                <button @click="removeRow(idx)" class="text-gray-300 hover:text-red-400 text-xs">✕</button>
              </td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
    <div class="flex gap-2">
      <button @click="addRow()" class="px-3 py-1.5 border border-gray-200 text-sm rounded-lg text-gray-500 hover:bg-gray-50">
        + 添加行
      </button>
      <button @click="savePositions()" class="px-4 py-1.5 bg-gray-900 text-white text-sm rounded-lg hover:bg-gray-700">
        保存持仓
      </button>
      <span x-show="saved" class="text-xs text-green-600 self-center">已保存</span>
    </div>
  </div>

  <!-- 本期计划（Task 8 实现） -->
  <div x-show="$store.page === 'plan'">
    <h1 class="text-lg font-medium mb-4">本期计划</h1>
    <div id="plan-content" class="text-sm text-gray-400">加载中...</div>
  </div>

  <!-- 榜单（Task 7 实现） -->
  <div x-show="$store.page === 'rankings'">
    <h1 class="text-lg font-medium mb-4">榜单</h1>
    <div id="rankings-content" class="text-sm text-gray-400">加载中...</div>
  </div>

</main>

<script>
  // Alpine store for active page
  document.addEventListener('alpine:init', () => {
    Alpine.store('page', 'account')
  })

  // 账户总览
  function accountPage() {
    return {
      snap: null,
      saved: false,
      form: {
        snapshot_date: new Date().toISOString().slice(0, 10),
        temperature: null, stock_total: null, stock_cash: null,
        bond_total: null, bond_cash: null, changqian_total: null,
        cash_pool: null, overseas_total: null,
      },
      async load() {
        const r = await fetch('/api/account/latest')
        this.snap = await r.json()
        if (this.snap) Object.assign(this.form, this.snap)
      },
      fmt(v) { return v == null ? '-' : (v/10000).toFixed(2) + '万' },
      async save() {
        await fetch('/api/account/snapshot', {
          method: 'POST', headers: {'Content-Type': 'application/json'},
          body: JSON.stringify(this.form),
        })
        this.saved = true
        await this.load()
        setTimeout(() => this.saved = false, 2000)
      },
    }
  }

  // 持仓
  function positionsPage() {
    return {
      tab: 'cb',
      cbRows: [], stockRows: [],
      dates: [], dateFilter: '',
      saved: false,
      async load() {
        const [cb, stock, cbDates, stockDates] = await Promise.all([
          fetch('/api/positions/cb').then(r => r.json()),
          fetch('/api/positions/stock').then(r => r.json()),
          fetch('/api/positions/cb/dates').then(r => r.json()),
          fetch('/api/positions/stock/dates').then(r => r.json()),
        ])
        this.cbRows = cb
        this.stockRows = stock
        this.dates = [...new Set([...cbDates, ...stockDates])].sort().reverse()
      },
      async loadByDate() {
        if (!this.dateFilter) return this.load()
        const [cb, stock] = await Promise.all([
          fetch(`/api/positions/cb?date=${this.dateFilter}`).then(r => r.json()),
          fetch(`/api/positions/stock?date=${this.dateFilter}`).then(r => r.json()),
        ])
        this.cbRows = cb; this.stockRows = stock
      },
      currentRows() { return this.tab === 'cb' ? this.cbRows : this.stockRows },
      addRow() { this.currentRows().push({code: '', name: '', shares: 0}) },
      removeRow(i) { this.currentRows().splice(i, 1) },
      async savePositions() {
        const today = new Date().toISOString().slice(0, 10)
        const strategy = this.tab
        const rows = this.currentRows().filter(r => r.code)
        await fetch(`/api/positions/${strategy}?position_date=${today}`, {
          method: 'POST', headers: {'Content-Type': 'application/json'},
          body: JSON.stringify(rows),
        })
        this.saved = true
        setTimeout(() => this.saved = false, 2000)
      },
    }
  }
</script>
</body>
</html>
```

- [ ] **Step 8: 注册前端路由（GET `/` → index.html）**

在 `app/main.py` 末尾追加：

```python
from fastapi.responses import FileResponse

@app.get("/")
def root():
    return FileResponse(STATIC_DIR / "index.html")
```

- [ ] **Step 9: 跑测试 + 手动验证**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_accounts -v
```

启动服务，打开 http://127.0.0.1:8000 ，验证：
- 账户总览页加载正常，卡片显示"暂无"
- 填写表单保存，页面刷新后显示数据
- 持仓页两个 tab 可切换，可添加/删除行

- [ ] **Step 10: commit**

```bash
git add app/ tests/test_api_accounts.py
git commit -m "feat: account and positions API + 账户总览/持仓 pages"
```

---

## Task 7: 榜单 API + 页面

**Files:**
- Create: `app/routers/rankings.py`
- Modify: `app/main.py` — include rankings router
- Modify: `app/static/index.html` — 填充榜单页内容
- Create: `tests/test_api_rankings.py`

**Interfaces:**
- Produces:
  - `GET /api/rankings/{strategy}/dates` → `list[str]`
  - `GET /api/rankings/{strategy}/latest` → `RankingsResponse`
  - `GET /api/rankings/{strategy}/{data_date}` → `RankingsResponse`
  - `GET /api/rankings/{strategy}/{data_date}/export` → `text/plain` (Markdown)
  - `RankingsResponse = { data_date, trade_date, items: list[dict] }`

- [ ] **Step 1: 写 API 测试**

```python
# tests/test_api_rankings.py
import unittest, sqlite3
from datetime import date
import pandas as pd
from fastapi.testclient import TestClient
from datasource import db

class TestRankingsApi(unittest.TestCase):
    def setUp(self):
        db._TEST_CONN = sqlite3.connect(":memory:")
        db._TEST_CONN.row_factory = sqlite3.Row
        db.init_db()
        run_id = db.insert_strategy_run("cb", date(2026, 6, 27))
        df = pd.DataFrame([{
            "bond_code": "113062", "bond_name": "常银转债",
            "cb_price": 127.74, "premium_rate": 13.29, "double_low": 141.03, "score": 0.824,
        }])
        db.insert_cb_rankings(run_id, df)
        from app.main import app
        self.client = TestClient(app)

    def tearDown(self):
        db._TEST_CONN.close()
        db._TEST_CONN = None

    def test_get_dates(self):
        r = self.client.get("/api/rankings/cb/dates")
        self.assertIn("2026-06-27", r.json())

    def test_get_latest(self):
        r = self.client.get("/api/rankings/cb/latest")
        data = r.json()
        self.assertEqual(data["data_date"], "2026-06-27")
        self.assertEqual(len(data["items"]), 1)
        self.assertEqual(data["items"][0]["bond_code"], "113062")

    def test_export_markdown(self):
        r = self.client.get("/api/rankings/cb/2026-06-27/export")
        self.assertEqual(r.status_code, 200)
        self.assertIn("常银转债", r.text)
        self.assertIn("| 排名 |", r.text)
```

- [ ] **Step 2: 确认测试失败**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_rankings -v 2>&1 | head -10
```

- [ ] **Step 3: 创建 `app/routers/rankings.py`**

```python
# app/routers/rankings.py
from __future__ import annotations
from typing import Literal
from fastapi import APIRouter, HTTPException
from fastapi.responses import PlainTextResponse
from datasource import db

router = APIRouter(prefix="/api/rankings", tags=["rankings"])


@router.get("/{strategy}/dates")
def get_dates(strategy: Literal["cb", "stock"]):
    return db.get_ranking_dates(strategy)


@router.get("/{strategy}/latest")
def get_latest(strategy: Literal["cb", "stock"]):
    dates = db.get_ranking_dates(strategy)
    if not dates:
        return {"data_date": None, "trade_date": None, "items": []}
    return _build_response(strategy, dates[0])


@router.get("/{strategy}/{data_date}/export")
def export_md(strategy: Literal["cb", "stock"], data_date: str):
    items = db.get_rankings(strategy, data_date)
    if not items:
        raise HTTPException(404, "榜单不存在")
    trade_date = items[0].get("trade_date", "")
    lines = [
        f"# {'转债' if strategy == 'cb' else '股票'} Top 20 榜单",
        f"- 数据日期：{data_date}  适用交易日：{trade_date}",
        "",
    ]
    if strategy == "cb":
        lines += ["| 排名 | 代码 | 名称 | 价格 | 溢价率 | 双低值 | 评分 |",
                  "|---|---|---|---:|---:|---:|---:|"]
        for r in items:
            lines.append(
                f"| {r['rank']} | {r['bond_code']} | {r['bond_name']} "
                f"| {r['cb_price']:.2f} | {r['premium_rate']:.2f}% "
                f"| {r['double_low']:.2f} | {r['score']:.3f} |"
            )
    else:
        lines += ["| 排名 | 代码 | 名称 | 总市值(亿) | PE | 扣非ROE |",
                  "|---|---|---|---:|---:|---:|"]
        for r in items:
            lines.append(
                f"| {r['rank']} | {r['stock_code']} | {r['stock_name']} "
                f"| {r['market_cap']:.2f} | {r['pe_ttm']:.2f} | {r['roe_ex']:.2f}% |"
            )
    return PlainTextResponse("\n".join(lines))


@router.get("/{strategy}/{data_date}")
def get_by_date(strategy: Literal["cb", "stock"], data_date: str):
    return _build_response(strategy, data_date)


def _build_response(strategy: str, data_date: str) -> dict:
    items = db.get_rankings(strategy, data_date)
    trade_date = items[0]["trade_date"] if items else None
    return {"data_date": data_date, "trade_date": trade_date, "items": items}
```

- [ ] **Step 4: 注册路由**（在 `app/main.py` 中追加）

```python
from app.routers import rankings
app.include_router(rankings.router)
```

- [ ] **Step 5: 跑测试**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_rankings -v
```
期望：3 tests passed

- [ ] **Step 6: 更新 index.html 的榜单页内容**

将 `index.html` 中 `<!-- 榜单（Task 7 实现） -->` 的 `<div>` 整体替换为：

```html
  <!-- 榜单 -->
  <div x-show="$store.page === 'rankings'" x-data="rankingsPage()" x-init="load()">
    <div class="flex justify-between items-center mb-4">
      <h1 class="text-lg font-medium">榜单</h1>
      <div class="flex gap-2 items-center">
        <select x-model="selectedDate" @change="loadByDate()" class="text-xs border border-gray-200 rounded px-2 py-1">
          <template x-for="d in dates"><option :value="d" x-text="d"></option></template>
        </select>
        <button @click="exportMd()" class="px-3 py-1.5 border border-gray-200 text-xs rounded-lg hover:bg-gray-50">
          导出 Markdown
        </button>
      </div>
    </div>
    <div x-show="tradeDate" class="text-xs text-green-600 mb-3"
      x-text="'基于 ' + selectedDate + ' 收盘数据 · 适用 ' + tradeDate + ' 执行'"></div>
    <div x-show="!items.length" class="text-sm text-gray-400 mb-4">
      暂无有效榜单，请运行策略：<code class="bg-gray-100 px-1 rounded">python3 -m strategies.cb_rotation.run</code>
    </div>
    <div class="flex gap-2 mb-4">
      <button @click="tab='cb'" :class="tab==='cb'?'bg-gray-900 text-white':'bg-white text-gray-500 border border-gray-200'"
        class="px-4 py-1.5 rounded-lg text-sm">转债 Top 20</button>
      <button @click="tab='stock'; loadStock()" :class="tab==='stock'?'bg-gray-900 text-white':'bg-white text-gray-500 border border-gray-200'"
        class="px-4 py-1.5 rounded-lg text-sm">股票 Top 20</button>
    </div>
    <!-- 转债表格 -->
    <div x-show="tab==='cb'" class="bg-white border border-gray-100 rounded-xl overflow-hidden">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 text-xs text-gray-400">
          <tr>
            <th class="text-center px-3 py-2 w-10">#</th>
            <th class="text-left px-3 py-2">代码</th>
            <th class="text-left px-3 py-2">名称</th>
            <th class="text-right px-3 py-2">价格</th>
            <th class="text-right px-3 py-2">溢价率</th>
            <th class="text-right px-3 py-2">双低值</th>
            <th class="text-right px-3 py-2">评分</th>
          </tr>
        </thead>
        <tbody>
          <template x-for="r in items" :key="r.bond_code">
            <tr class="border-t border-gray-50">
              <td class="text-center px-3 py-2 text-gray-400 text-xs" x-text="r.rank"></td>
              <td class="px-3 py-2 text-gray-500" x-text="r.bond_code"></td>
              <td class="px-3 py-2" x-text="r.bond_name"></td>
              <td class="px-3 py-2 text-right" x-text="r.cb_price?.toFixed(2)"></td>
              <td class="px-3 py-2 text-right" x-text="r.premium_rate?.toFixed(2) + '%'"></td>
              <td class="px-3 py-2 text-right" x-text="r.double_low?.toFixed(2)"></td>
              <td class="px-3 py-2 text-right" x-text="r.score?.toFixed(3)"></td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
    <!-- 股票表格 -->
    <div x-show="tab==='stock'" class="bg-white border border-gray-100 rounded-xl overflow-hidden">
      <table class="w-full text-sm">
        <thead class="bg-gray-50 text-xs text-gray-400">
          <tr>
            <th class="text-center px-3 py-2 w-10">#</th>
            <th class="text-left px-3 py-2">代码</th>
            <th class="text-left px-3 py-2">名称</th>
            <th class="text-right px-3 py-2">总市值(亿)</th>
            <th class="text-right px-3 py-2">PE</th>
            <th class="text-right px-3 py-2">扣非ROE</th>
          </tr>
        </thead>
        <tbody>
          <template x-for="r in stockItems" :key="r.stock_code">
            <tr class="border-t border-gray-50">
              <td class="text-center px-3 py-2 text-gray-400 text-xs" x-text="r.rank"></td>
              <td class="px-3 py-2 text-gray-500" x-text="r.stock_code"></td>
              <td class="px-3 py-2" x-text="r.stock_name"></td>
              <td class="px-3 py-2 text-right" x-text="r.market_cap?.toFixed(2)"></td>
              <td class="px-3 py-2 text-right" x-text="r.pe_ttm?.toFixed(2)"></td>
              <td class="px-3 py-2 text-right" x-text="r.roe_ex?.toFixed(2) + '%'"></td>
            </tr>
          </template>
        </tbody>
      </table>
    </div>
  </div>
```

在 `<script>` 末尾追加：

```javascript
  function rankingsPage() {
    return {
      tab: 'cb', items: [], stockItems: [],
      dates: [], selectedDate: '', tradeDate: '',
      async load() {
        const [cbDates, stockDates] = await Promise.all([
          fetch('/api/rankings/cb/dates').then(r => r.json()),
          fetch('/api/rankings/stock/dates').then(r => r.json()),
        ])
        this.dates = [...new Set([...cbDates, ...stockDates])].sort().reverse()
        this.selectedDate = this.dates[0] || ''
        if (this.selectedDate) await this.loadByDate()
      },
      async loadByDate() {
        if (!this.selectedDate) return
        const r = await fetch(`/api/rankings/cb/${this.selectedDate}`).then(r => r.json())
        this.items = r.items; this.tradeDate = r.trade_date || ''
        if (this.tab === 'stock') await this.loadStock()
      },
      async loadStock() {
        if (!this.selectedDate) return
        const r = await fetch(`/api/rankings/stock/${this.selectedDate}`).then(r => r.json())
        this.stockItems = r.items
      },
      async exportMd() {
        const strategy = this.tab
        const r = await fetch(`/api/rankings/${strategy}/${this.selectedDate}/export`)
        const text = await r.text()
        const blob = new Blob([text], {type: 'text/plain'})
        const a = document.createElement('a')
        a.href = URL.createObjectURL(blob)
        a.download = `${strategy}_top20_${this.selectedDate}.md`
        a.click()
      },
    }
  }
```

- [ ] **Step 7: 手动验证**

启动服务，打开 http://127.0.0.1:8000，切换到榜单页，验证：
- 跑完策略后榜单自动显示
- 日期切换器有历史数据
- 导出按钮下载 `.md` 文件
- 转债/股票 tab 切换正常

- [ ] **Step 8: commit**

```bash
git add app/routers/rankings.py tests/test_api_rankings.py app/static/index.html app/main.py
git commit -m "feat: rankings API + 榜单 page with export"
```

---

## Task 8: 本期计划 API + 页面

**Files:**
- Create: `app/routers/plans.py`
- Modify: `app/main.py` — include plans router
- Modify: `app/static/index.html` — 填充本期计划页
- Create: `tests/test_api_plans.py`

**Interfaces:**
- Consumes: `datasource.db.get_latest_orders`, `get_latest_rankings`, `get_latest_account_snapshot`; `rebalance.calc_targets`, `rebalance.build_transfer_plan`
- Produces: `GET /api/plan` → `PlanResponse = { generated_at, account, transfer_steps, cb: {data_date, orders}, stock: {data_date, orders}, missing }`

- [ ] **Step 1: 写测试**

```python
# tests/test_api_plans.py
import unittest, sqlite3
from datetime import date
import pandas as pd
from fastapi.testclient import TestClient
from datasource import db

class TestPlansApi(unittest.TestCase):
    def setUp(self):
        db._TEST_CONN = sqlite3.connect(":memory:")
        db._TEST_CONN.row_factory = sqlite3.Row
        db.init_db()
        db.insert_account_snapshot("2026-06-29", 45.0, 209555, 274, 227183, 110, 110606, 59013, 93030)
        run_id = db.insert_strategy_run("cb", date(2026, 6, 27))
        db.insert_cb_orders(run_id, pd.DataFrame([{
            "action": "BUY", "bond_code": "123150", "bond_name": "九强转债",
            "price": 128.67, "delta_shares": 90, "amount": 11580.3,
        }]))
        run_id2 = db.insert_strategy_run("stock", date(2026, 6, 29))
        db.insert_stock_orders(run_id2, pd.DataFrame([{
            "action": "SELL", "stock_code": "600051", "stock_name": "宁波联合",
            "price": 5.68, "delta_shares": -1800, "amount": 10224.0,
        }]))
        from app.main import app
        self.client = TestClient(app)

    def tearDown(self):
        db._TEST_CONN.close()
        db._TEST_CONN = None

    def test_get_plan_returns_all_sections(self):
        r = self.client.get("/api/plan")
        data = r.json()
        self.assertIn("transfer_steps", data)
        self.assertIn("cb", data)
        self.assertIn("stock", data)
        self.assertGreater(len(data["cb"]["orders"]), 0)
        self.assertGreater(len(data["stock"]["orders"]), 0)

    def test_missing_when_no_data(self):
        db._TEST_CONN.execute("DELETE FROM account_snapshots")
        r = self.client.get("/api/plan")
        self.assertIn("account", r.json()["missing"])
```

- [ ] **Step 2: 确认测试失败**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_plans -v 2>&1 | head -10
```

- [ ] **Step 3: 创建 `app/routers/plans.py`**

```python
# app/routers/plans.py
from __future__ import annotations
from datetime import datetime
from fastapi import APIRouter
from datasource import db

router = APIRouter(prefix="/api/plan", tags=["plan"])


@router.get("")
def get_plan():
    missing = []
    account = db.get_latest_account_snapshot()
    if not account:
        missing.append("account")

    cb_orders = db.get_latest_orders("cb")
    cb_dates = db.get_ranking_dates("cb")
    if not cb_orders:
        missing.append("cb_orders")

    stock_orders = db.get_latest_orders("stock")
    stock_dates = db.get_ranking_dates("stock")
    if not stock_orders:
        missing.append("stock_orders")

    transfer_steps = []
    if account:
        try:
            import sys
            from pathlib import Path
            sys.path.insert(0, str(Path(__file__).resolve().parents[2]))
            from rebalance import calc_targets, build_transfer_plan, _THRESHOLD
            domestic = (
                account["stock_total"] + account["bond_total"]
                + account["changqian_total"] + account["cash_pool"]
            )
            targets = calc_targets(domestic, account["temperature"])
            deltas = {
                "stock":     targets["stock"]     - account["stock_total"],
                "bond":      targets["bond"]      - account["bond_total"],
                "changqian": targets["changqian"] - account["changqian_total"],
                "cash_pool": targets["cash_pool"] - account["cash_pool"],
            }
            transfer_steps = build_transfer_plan(deltas)
        except Exception:
            transfer_steps = []

    return {
        "generated_at": datetime.now().isoformat(),
        "missing": missing,
        "account": account,
        "transfer_steps": transfer_steps,
        "cb": {
            "data_date": cb_dates[0] if cb_dates else None,
            "orders": cb_orders,
        },
        "stock": {
            "data_date": stock_dates[0] if stock_dates else None,
            "orders": stock_orders,
        },
    }
```

- [ ] **Step 4: 注册路由**

```python
# app/main.py 追加
from app.routers import plans
app.include_router(plans.router)
```

- [ ] **Step 5: 跑测试**

```bash
source .venv/bin/activate && python3 -m unittest tests.test_api_plans -v
```
期望：2 tests passed

- [ ] **Step 6: 更新 index.html 的本期计划页**

将 `<!-- 本期计划（Task 8 实现） -->` 的 `<div>` 替换为：

```html
  <!-- 本期计划 -->
  <div x-show="$store.page === 'plan'" x-data="planPage()" x-init="load()">
    <div class="flex justify-between items-center mb-4">
      <h1 class="text-lg font-medium">本期计划</h1>
      <span x-show="plan?.cb?.data_date" class="text-xs text-green-600"
        x-text="'适用 ' + (plan?.stock?.data_date || plan?.cb?.data_date || '') + ' 执行'"></span>
    </div>

    <!-- 缺数据提示 -->
    <div x-show="plan?.missing?.length" class="bg-amber-50 border border-amber-200 rounded-xl p-3 mb-4 text-sm text-amber-700">
      缺少数据：<span x-text="plan?.missing?.join('、')"></span>，请先录入账户信息或运行策略。
    </div>

    <!-- 资金调拨 -->
    <div class="bg-white border border-gray-100 rounded-xl p-4 mb-3" x-show="plan?.transfer_steps?.length">
      <div class="text-sm font-medium mb-2">① 资金调拨</div>
      <template x-for="(step, i) in plan?.transfer_steps || []">
        <div class="text-sm text-gray-600 py-1 border-b border-gray-50 last:border-0"
          x-text="(i+1) + '. ' + step"></div>
      </template>
    </div>

    <!-- 转债订单 -->
    <div class="bg-white border border-gray-100 rounded-xl p-4 mb-3" x-show="plan?.cb?.orders?.length">
      <div class="text-sm font-medium mb-2">② 转债订单
        <span class="text-xs text-gray-400 font-normal ml-2" x-text="'基于 ' + plan?.cb?.data_date"></span>
      </div>
      <template x-for="group in cbGroups()">
        <div class="mb-3">
          <div class="text-xs text-gray-400 mb-1" x-text="group.label + ' (' + group.items.length + ')'"></div>
          <template x-for="r in group.items">
            <div class="flex justify-between text-sm py-1 border-b border-gray-50">
              <span><span class="text-gray-400 mr-2" x-text="r.bond_code"></span><span x-text="r.bond_name"></span></span>
              <span class="text-gray-500">
                <span :class="r.shares>0?'text-green-600':'text-red-500'"
                  x-text="(r.shares>0?'+':'')+r.shares+'张'"></span>
                <span class="ml-2 text-gray-400" x-text="'¥'+Math.abs(r.amount).toLocaleString()"></span>
              </span>
            </div>
          </template>
        </div>
      </template>
    </div>

    <!-- 股票订单 -->
    <div class="bg-white border border-gray-100 rounded-xl p-4" x-show="plan?.stock?.orders?.length">
      <div class="text-sm font-medium mb-2">③ 股票订单
        <span class="text-xs text-gray-400 font-normal ml-2" x-text="'基于 ' + plan?.stock?.data_date"></span>
      </div>
      <template x-for="group in stockGroups()">
        <div class="mb-3">
          <div class="text-xs text-gray-400 mb-1" x-text="group.label + ' (' + group.items.length + ')'"></div>
          <template x-for="r in group.items">
            <div class="flex justify-between text-sm py-1 border-b border-gray-50">
              <span><span class="text-gray-400 mr-2" x-text="r.stock_code"></span><span x-text="r.stock_name"></span></span>
              <span class="text-gray-500">
                <span :class="r.shares>0?'text-green-600':r.shares<0?'text-red-500':'text-gray-400'"
                  x-text="(r.shares>0?'+':'')+r.shares+'股'"></span>
                <span class="ml-2 text-gray-400" x-text="r.amount?'¥'+Math.abs(r.amount).toLocaleString():''"></span>
              </span>
            </div>
          </template>
        </div>
      </template>
    </div>
  </div>
```

在 `<script>` 末尾追加：

```javascript
  function planPage() {
    const ORDER = {SELL:'卖出', BUY:'买入', ADD:'加仓', TRIM:'减仓', HOLD:'持有', SKIP:'跳过'}
    return {
      plan: null,
      async load() {
        this.plan = await fetch('/api/plan').then(r => r.json())
      },
      cbGroups() {
        const orders = this.plan?.cb?.orders || []
        return ['SELL','TRIM','BUY','ADD','HOLD'].map(action => ({
          label: ORDER[action], action,
          items: orders.filter(r => r.action === action)
        })).filter(g => g.items.length)
      },
      stockGroups() {
        const orders = this.plan?.stock?.orders || []
        return ['SELL','BUY','ADD','TRIM','HOLD','SKIP'].map(action => ({
          label: ORDER[action], action,
          items: orders.filter(r => r.action === action)
        })).filter(g => g.items.length)
      },
    }
  }
```

- [ ] **Step 7: 手动验证**

启动服务，切换到本期计划页，验证：
- 三个区块（资金调拨 / 转债订单 / 股票订单）正常渲染
- 缺数据时显示提示
- 订单按 action 分组显示，买卖金额正确

- [ ] **Step 8: 全量测试**

```bash
source .venv/bin/activate && python3 -m unittest discover tests/ -v
```
期望：全部通过

- [ ] **Step 9: commit**

```bash
git add app/routers/plans.py tests/test_api_plans.py app/static/index.html app/main.py
git commit -m "feat: trade plan API + 本期计划 page"
```

---

## Self-Review

**Spec coverage 检查：**
- ✅ 账户总览（含历史曲线占位）：Task 6
- ✅ 持仓差量录入：Task 6
- ✅ 本期计划（资金调拨 + 转债订单 + 股票订单）：Task 8
- ✅ 榜单（转债/股票 Top 20，日期回溯，导出）：Task 7
- ✅ 历史留痕（账户/持仓/榜单/订单均写库）：Tasks 1-4
- ✅ 三件事独立（分别入口，plan API 仅在数据齐全时输出 transfer_steps）：Task 8
- ✅ 数据录入（账户表单 + 持仓差量）：Task 6
- ⚠️ 账户总览历史曲线：占位（`<div id="history-chart">`）后续用 Chart.js 实现，不在本期范围

**类型一致性：** `strategy` 参数在所有 router 中均为 `Literal["cb", "stock"]`；DB 函数签名与 router 调用处一致。

**遗漏项：** 无，所有 spec 功能均有对应 Task。

---

> 计划完成后使用 `/stock-rebalance` 或 `/rebalance` 验证策略仍能正常运行并写入数据库。
