# Convertible Bond Data Probe Implementation Plan

> 状态：历史实施计划，不是当前实现真源。

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a read-only command that reports current convertible-bond data-source availability.

**Architecture:** Keep the probe independent from strategy execution. Use small
pure helpers for missing rates, field support, momentum, and Markdown rendering;
wrap each external source independently so enhanced-source failures cannot stop
the core report.

**Tech Stack:** Python 3, pandas, AKShare, python-dotenv, optional Tushare.

---

### Task 1: Pure Helper Tests

**Files:**
- Create: `tests/test_cb_data_probe.py`
- Create: `strategies/convertible_bond/data_probe.py`

- [ ] Write tests for missing rates, momentum, capability detection, report
  redaction, and CLI sample-size parsing.
- [ ] Run `python3 -m unittest tests.test_cb_data_probe -v` and confirm the new
  module is missing.
- [ ] Implement the minimal pure helpers.
- [ ] Run the focused tests and confirm they pass.

### Task 2: External Probes And Report

**Files:**
- Create: `strategies/convertible_bond/__init__.py`
- Modify: `strategies/convertible_bond/data_probe.py`

- [ ] Implement independent AKShare, JSL, Tushare, and stock-momentum probes.
- [ ] Write `data/output/cb_data_probe_report.md` even when the core probe fails.
- [ ] Make the CLI return non-zero only when the core AKShare probe fails.

### Task 3: Credential Safety And Verification

**Files:**
- Create: `.env.example`
- Modify: `.gitignore`
- Modify: `requirements.txt`

- [ ] Add `python-dotenv` as a core dependency and keep Tushare optional.
- [ ] Ignore `.env`, `*.cookie`, and `secrets/`.
- [ ] Run unit tests.
- [ ] Run the real probe and inspect the Markdown report for source status,
  field availability, momentum sample coverage, and credential redaction.
