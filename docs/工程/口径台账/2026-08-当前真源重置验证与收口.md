# 当前真源 v2026-08 重置验证与收口

> 状态：当前真源重置收口说明
> 职责：记录 v2026-08 文档口径重置包的导航、证据元数据、阶段性语言和补丁卫生验证结果。
> 真源范围：本轮 #2 至 #11 纯文档重置工作的可审查性。
> 不负责：重新定义投资规则、修改工具实现、评估策略收益或替代测试报告。
> 对应票据：#11 文档口径重置验证与收口
> 最后核对：2026-08-05

## 1. 收口范围

本次收口覆盖当前分支相对远端的文档重置包：

- 当前真源重置基线盘点。
- 当前真源 v2026-08 口径台账与分类规则。
- 五份当前真源文档重写：投资体系总纲、仓位与再平衡规则、交易执行规则、小市值股票策略规则、多因子可转债策略规则。
- 导航入口更新：根 README、docs/README、体系 README、策略 README、工程 README。
- 当前真源 v2026-08 实现差距清单。

规则真源仍只由对应体系和策略文档承担；本文件只记录验证结果。

## 2. 已运行检查

| 检查 | 命令或范围 | 结果 |
| --- | --- | --- |
| Markdown 本地链接 | 扫描仓库内全部 `.md`，排除 `.git` 与 `.venv` | 通过；检查 `170` 个 Markdown 文件，本地链接均可解析 |
| 证据元数据解析 | 扫描 `docs/**/证据/` 下的 `.json` 和 `.csv` | 通过；`10` 个 JSON 可解析，`71` 个 CSV 有有效表头并可读取 |
| 阶段性语言 | 检查当前导航、五份当前真源、口径台账和实现差距清单中的 `MVP`、`V0`、`R1`、草案、候选、废弃、旧公式、验证中 | 通过；出现位置均属于明确状态、执行边界、策略候选语义或口径台账解释 |
| 补丁卫生 | `git diff --name-only origin/agent/ark-plan-health-check...HEAD` | 通过；本轮重置包只改 README 与 `docs/` 下 Markdown 文档 |

## 3. 阶段性语言结论

阶段性语言没有被清空，因为它们在当前文档中承担边界说明职责：

| 词汇 | 当前允许语境 | 收口判断 |
| --- | --- | --- |
| `MVP` | 工程边界、人在回路、计划生成而非自动执行 | 已由系统定位、交易执行规则和实现差距清单约束 |
| `V0` | 多因子可转债当前执行版本；有效性仍验证中 | 已明确不能证明长期超额，也不能替代未来验证 |
| `R1` | 可转债研究材料或口径台账中的研究阶段名 | 不作为当前规则版本 |
| 草案 | 交易计划、订单草案等计划输出语义 | 不表示当前真源仍是草案 |
| 候选 | 策略候选、可转债候选不足安全阀、研究候选材料 | 当前规则中均有边界，研究候选不能直接晋升 |
| 废弃 | 旧公式、旧路径或被新结构取代的口径 | 已集中在口径台账、仓位规则历史段和实现差距中 |
| 旧公式 | 旧国内四部分等历史口径 | 已降级为废弃或仅历史，不进入当前主路径 |
| 验证中 | 参数、策略有效性、字段语义或数据稳定性尚未定型 | 已要求说明验证对象，不能写成长期定论 |

历史设计、研究流水和证据附件中仍可能出现这些词。那些文件的目录职责已经说明其状态为历史、研究或证据材料；它们是有意保留的历史例外，不参与当前真源排序。

## 4. 补丁卫生结论

本轮文档口径重置没有修改应用、API、数据库结构、策略 runner 或 UI。相对远端的文件清单全部位于：

- `README.md`
- `docs/README.md`
- `docs/体系/`
- `docs/策略/`
- `docs/工程/`

因此本轮审查应按纯文档包处理。后续工程实现应从 [当前真源 v2026-08 实现差距清单](../实现状态/当前真源-v2026-08-实现差距清单.md) 拆票，而不是把本轮文档提交当成代码变更。

## 5. 测试说明

#10 提交前已经在同一代码状态下运行完整业务测试：

```text
.venv/bin/python -m unittest discover -s tests -v
Ran 240 tests
OK
```

#11 只新增本收口说明和目录索引，不改变 Python、HTML、API、数据库 schema、策略 runner、配置或测试文件。因此本票据不需要重新运行完整业务测试；本次收口用链接解析、证据元数据解析、阶段性语言检查和补丁卫生检查覆盖其验收目标。

## 6. 收口判断

当前真源 v2026-08 文档包已经可以作为后续规则阅读和工程拆票的审查基线：

- 当前真源入口可导航。
- 证据附件的 JSON/CSV 元数据仍可解析。
- 阶段性语言被限制在明确状态或历史、研究、实现边界内。
- 补丁保持纯文档范围。
- 工具实现差距已单独登记，后续不需要从旧口径或页面便利行为倒推规则。
