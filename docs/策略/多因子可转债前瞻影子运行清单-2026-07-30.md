# 多因子可转债前瞻影子运行清单（2026-07-30）

> 状态：待执行。本文只服务 `CB-SHADOW-20260720-v1` 的第一期有效快照准备，不生成真实订单，不修改 V0、正式策略规则、生产代码、配置或数据库。

## 1. 本期目标

在 2026-07-30 收盘后、下一交易日开盘前，完整封存 `B0_BASE/C_BAL/C_OFF` 三条路线的同一可投资域、同一免费输入、同一风险过滤、同一 Top20 排名和来源哈希。若不能完整封存，本期直接记为 `invalid`，不事后补榜。

本期只回答一个问题：免费输入和影子记录流程是否可以按时、可复核、无未来函数地运行。不得回答哪条候选更好，也不得据此调整参数。

## 2. 固定候选

| 候选 | 角色 | 排名公式 | 当前用途 |
| --- | --- | --- | --- |
| `B0_BASE` | 经典双低永久基线 | `转债全价 + 转股溢价率百分数` 升序 | 衡量复杂度是否有增量 |
| `C_BAL` | 第一历史研究假设 | 含信号日在内 252 个有效交易日的双低 Z 分数升序 | 当前免费均衡候选 |
| `C_OFF` | 探索性偏进攻挑战者 | 双低、252 日双低 Z、剩余面额三个横截面百分位等权相加，升序 | 只检验进攻增强是否值得防守代价 |

三条路线必须同期保留。不得因为某一条当期榜单更顺眼而少记录另一条。

## 3. 收盘后封存清单

| 项目 | 最小内容 | 免费来源或复算路径 | 失效条件 |
| --- | --- | --- | --- |
| `manifest` | 协议版本、信号日、抓取完成时间、来源清单、原始快照哈希、有效状态 | 本地生成 | 未在下一开盘前完成或缺来源哈希 |
| 证券主表 | 转债/正股代码、名称、上市/退市、合同到期、转股起止、发行规模 | 已冻结主表、交易所、东方财富/同花顺交叉 | 映射冲突无法裁决 |
| 转债行情 | 信号日全价收盘、开高低收、成交量额、停牌/无成交状态 | 新浪/东方财富，成交额优先交易所路径 | 缺收盘价或状态不明 |
| 转股价与溢价 | 当日已生效转股价、转股价值、转股溢价率、双低 | 公告/历史转股价事件 + 正股价复算 | 使用当前转股价回填历史 |
| `F02` | 含当日 252 个有效交易日双低 Z 分数 | 本地复算 | 有效历史不足 252 日则不得进 `C_BAL/C_OFF` |
| 余额 | 最近已公开且已生效剩余面额，纳入已知回售结果 | 沪深交易所转股统计 + 回售公告 | 余额时点不明或单位冲突 |
| 风险状态 | 已公告强赎、评级/风险警示、重整、兑付、终止上市、剩余期限 | 巨潮/交易所/集思录当前交叉 | 状态不明则排除并进人工队列 |
| 排名与 Top20 | 三条路线全域排名、并列按代码升序、Top20 标签 | 本地复算 | 三条路线没有同域同日同时封存 |

## 4. 下一开盘后记录清单

| 项目 | 最小内容 | 口径 |
| --- | --- | --- |
| 虚拟执行 | 买入、卖出、旧仓锁定、无法买入留现金、风险退出 | 下一交易日真实开盘价，单边 10bp |
| 可成交性 | 无开盘、停牌、涨跌停/临停、首笔时间、首分钟成交金额 | 只记录事实，不用收盘价替代开盘 |
| 组合状态 | 每条候选独立现金、锁资、持仓、费用和净值 | 候选之间不得共用现金或头寸 |
| 异常队列 | 来源冲突、风险状态不明、映射冲突、字段缺失 | 先排除或记无效，后续修复不得回填当期 |

## 5. 禁止项

- 不新增因子、窗口、权重、持仓数或调仓频率。
- 不继续社区检索或研报扩张，除非当前清单发现精确缺口。
- 不打开 RG1、LSMC、隐含波动率、深度学习或付费数据支线。
- 不把影子榜单写入交易计划，不影响 V0 实盘。
- 不用短期收益评价候选优劣；第 1—13 个有效信号只评价数据与执行流程。

## 6. 当前待办

- [x] 2026-07-29 前完成免费来源可用性预检查，只记录接口/页面是否可访问，不生成榜单。
- [x] 2026-07-24 完成只读快照入口护栏演练：非信号日无 `--dry-run` 必须失败，`--dry-run` 只生成 rehearsal manifest 与 source checks。
- [ ] 2026-07-30 收盘后封存 `manifest` 和 `universe`。
- [ ] 2026-07-31 开盘后封存 `execution`。
- [ ] 2026-07-31 收盘后生成第一日 `portfolio/benchmark/review`。
- [ ] 更新研究看板与验证记录，明确本期 `valid/invalid` 和原因。

## 7. 2026-07-23 本地预检结论

现有 `CB-REPRO-20260720-v1` 冻结包包含 947 只券、715,800 行日频面板，覆盖 2018-01-02 至 2026-07-17。面板字段包括 `bond_code/date/open_sina/close_em/conv_premium/double_low/double_low_z252/balance_bil/contract_maturity/call_notice_date/tail_risk_date/st_risk_active`，可以复用为排名公式、字段命名和历史尾部风险合同的模板。

但该冻结包不能直接作为 2026-07-30 前瞻快照，因为它的最后日期早于信号日，且来源哈希来自历史整改包而非当期收盘后抓取。2026-07-30 必须重新取得或复算以下当期字段：转债收盘/开盘、转股溢价率、252 日双低历史位置、剩余面额、剩余期限、已公告强赎、硬信用风险、ST/停牌/可交易状态和来源哈希。

本地预检没有发现必须付费才能进入第一期影子记录的字段；真正的未解问题是当期免费源是否稳定、信号日后能否及时封存，以及下一开盘的可成交深度如何记录。

## 8. 2026-07-23 免费源可用性预检

本次只做只读联网预检，不生成榜单、不保存市场快照、不比较候选收益。预检结果见[20260730 影子免费源预检查表](证据/可转债-20260730影子免费源预检查.csv)。

结果分三层：

1. 可作为 2026-07-30 主路径：新浪转债当前快照、集思录强赎/当前状态、转债历史日线、正股单券历史日线、本地 Python 环境。
2. 可用但有速度或维护风险：`stock_zh_a_spot` 能返回全市场 5,529 行，但耗时约 31.5 秒；可作全市场正股状态备份，不作为唯一封存路径。
3. 当前不稳定：东方财富 `stock_zh_a_spot_em` 与 `stock_zh_a_st_em` 在只读预检中失败；转债分钟接口样本失败。它们不构成付费理由，但要求 2026-07-30/31 对候选入选券准备单券日线、集思录状态、交易所状态、名称风险警示和公告人工队列。

本预检没有发现第一期影子快照必须采购 Tushare、禄得、集思录会员或其他付费数据。剩余风险是免费源在信号日当晚的稳定性、ST/风险状态交叉和开盘首分钟成交深度记录。

## 9. 2026-07-23 历史回放 dry-run

可以使用历史数据验证影子记录工具链，但必须明确其身份：历史回放只验证格式、字段、排名复算和失效规则，不能计入前瞻样本，也不能比较候选收益。

已新增只读脚本 `scripts/research/cb_strategy_repro/historical_shadow_dry_run.py`，默认读取 `CB-REPRO-20260720-v1` 冻结包，选择冻结包内最后一个完整历史信号日。首次运行选择 `2026-07-10` 收盘信号、`2026-07-13` 下一开盘执行日，输出：

- [历史 dry-run manifest](证据/可转债-历史影子dryrun-20260710-manifest.json)
- [历史 dry-run universe](证据/可转债-历史影子dryrun-20260710-universe.csv)
- [历史 dry-run execution](证据/可转债-历史影子dryrun-20260710-execution.csv)
- [历史 dry-run portfolio](证据/可转债-历史影子dryrun-20260710-portfolio.csv)
- [历史 dry-run review](证据/可转债-历史影子dryrun-20260710-review.csv)

本次 dry-run 结果：`data_status=dry_run_invalid_for_forward_sample`；共同安全域 240 只；`B0_BASE/C_BAL/C_OFF` 可排名数分别为 `240/187/187`；三条路线各自 Top20 完整生成，Top20 并集 40 只；下一开盘虚拟执行表 60 行，开盘缺失留现金 0 行；组合摘要 3 行，review 有效观察数固定为 0。`C_BAL/C_OFF` 少于共同安全域，主要来自 252 日历史位置和余额等增强字段要求；这正是前瞻封存时需要显式呈现的字段缺口，而不是事后删样本。

本关说明：7 月 30 前不是无事可做，可以继续用历史回放验工具链；但所有历史回放输出都必须带 `dry_run_invalid_for_forward_sample` 标签，不能混入 `CB-SHADOW-20260720-v1` 的有效观察数。

## 10. 2026-07-24 只读快照入口护栏

已新增 `scripts/research/cb_strategy_repro/shadow_snapshot.py`，作为 2026-07-30 前瞻影子快照的入口护栏。该脚本当前只负责生成 `manifest` 与 `source-checks`，不抓市场快照、不生成榜单、不写数据库、不生成订单。

护栏规则：

1. 非 10/20/30 日默认不能生成非 dry-run manifest；
2. 显式 `--dry-run` 时，只允许输出 `dry_run_invalid_for_forward_sample`；
3. manifest 固定写入 `generates_orders=false`、`writes_database=false`、`modifies_v0=false`；
4. 有效观察数在 dry-run 中固定为 `0`；
5. source checks 只列当期要跑的主路径和人工风险队列，不把未运行接口伪装成已可用。

2026-07-24 演练输出：

- [影子快照入口 dry-run manifest](证据/可转债-影子快照入口-20260724-manifest.json)
- [影子快照入口 source checks](证据/可转债-影子快照入口-20260724-source-checks.csv)

命令验证：`2026-07-24` 不带 `--dry-run` 会因非信号日退出；带 `--dry-run` 可生成 rehearsal 文件，且 `data_status=dry_run_invalid_for_forward_sample`。下一步不是提前生成 `universe`，而是在 2026-07-30 收盘后把当前入口接到实际只读采集和封存流程。

## 11. 2026-07-24 来源探测层接入

`shadow_snapshot.py` 已新增 `--probe-sources`，只运行轻量只读探测，并把结果写入 `source-checks` 与 manifest 的 `source_summary`。该模式只保存来源状态、行数、列名、耗时和错误摘要，不保存任何市场原始行，不生成 `universe`，不生成订单。

同日演练分两步：

1. 沙箱内运行时，三条自动源均因本机代理权限失败；这说明脚本能把连接失败写入状态摘要。
2. 授权只读联网后，来源探测能写入真实状态摘要：新浪转债当前快照可用，本次返回 329 行；正股单券历史日线在东方财富路径波动时自动切到 `stock_zh_a_daily` 备份并返回 4 行；集思录强赎/当前状态可用，返回 318 行；人工风险队列仍标记 `not_run`。

当前结论：7 月 30 当晚入口层已经能区分“源站可用”“非阻断来源失败”“沙箱/网络失败”和“人工队列未运行”，并支持正股单券免费备份路径。下一步才是把这些来源状态接到当期 `universe` 封存逻辑；在 7 月 30 收盘前仍不得提前生成有效榜单。

## 12. 2026-07-24 universe 封存骨架

`shadow_snapshot.py` 已新增 `--write-universe-template`。该模式只写空 `universe` 模板和人工风险队列模板，不写任何候选券、不保存行情原始行、不生成 Top20。模板用于提前固定 7 月 30 当晚必须产出的列和阻断条件。

2026-07-24 rehearsal 输出：

- [universe 空模板](证据/可转债-影子快照入口-20260724-universe-template.csv)：0 行、23 列；
- [人工风险队列模板](证据/可转债-影子快照入口-20260724-manual-risk-queue.csv)：0 行、9 列；
- manifest 中 `universe_collection.status=blocked_by_source_checks`，阻断源为 `manual_risk_queue`。

授权只读探测后的自动源状态为：新浪转债当前快照 available、集思录强赎/当前状态 available、正股单券日线 available 且使用 `stock_zh_a_daily` 备份；人工风险队列仍 `not_run`。这意味着 7 月 30 前的自动入口骨架已经准备好，但有效 `universe` 必须等信号日收盘后，并且人工风险队列完成后才允许封存。
