{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.py) .venv/bin/ruff check --fix \"$f\";; esac; } 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "ruff check..."
          }
        ]
      }
    ]
  }
}
